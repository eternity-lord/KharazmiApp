# FIX grade-289 (options 1+3) — implemented 2026-09-13, NO compile/run

## Step 1 — App (ClassDetailActivity.kt)
- Choice: (b) send null (omit field). (a) was impossible without a server change:
  `ClassReportInfo` (:36-40) has NO grade field, so "current grade" isn't available.
  Note: "both Optional" referred to the SERVER Pydantic model; the Kotlin model
  was non-null and had to be made nullable — still a 1-line change.
- :135 `data class ClassUpdateInfo(val title: String, val grade_level: String? = null)`
- :289-292 call site → `ClassUpdateInfo(newTitle)` + FIX comment. Only construction
  site in the app (grep-verified). Gson default (`GsonConverterFactory.create()`,
  RetrofitClient.kt:123) omits nulls → JSON contains title only → Pydantic None → "don't touch".
- :187 comment updated: button is title-only now (was «(پایه و عنوان)»).

## Step 2 — Server (routers/classes.py)
- :1080-1091 `_VALID_GRADE_LEVELS`: L13 tariff list (financial_calculations.py:244-250)
  PLUS creation-legit non-tariff values «کنکور/فارغ‌التحصیل/سایر» (AddClass spinner).
  Deliberate extension: strict-L13-only would 400 the seed class's own grade (کنکور)
  and any future grade-picker offering it. L13 maps these to category=None with a
  clear error only when fallback pricing is actually needed — same philosophy here.
- :1116-1122 validation inside the existing non-empty guard: same normalization as
  L13 (ي→ی, ك→ک, ZWNJ→space, collapse); unknown → HTTP 400
  «مقطع تحصیلی «X» نامعتبر است», raised BEFORE any assignment/commit.
  Empty/whitespace keeps old "skip" semantics; valid values stored raw-stripped (unchanged).

## Step 3 — Manual trace (grade «نهم» title edit)
Dialog → updateClassInfoOnServer(T) → body {"title":T} → grade_level=None →
server skips grade block → commit → title changed, grade stays «نهم». ✓
Invalid «xyz» → 400, nothing written. ✓ Explicit valid «نهم» → stored. ✓

## Verification: re-read edited regions + `ast.parse` of classes.py (static syntax only).
