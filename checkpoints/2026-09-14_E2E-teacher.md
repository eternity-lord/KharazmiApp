# E2E teacher test (2026-09-14; COPY; hash-identical; /tmp cleaned; report-only, no fixes)

## Verdict: money core EXACT, IDOR wall SOLID, 3 findings (1 real bug + 2 boundary decisions)

## Money verified exact
- Mixed session (Present/Late/Absent): T=200000 (100000×2, class rate), I=80000 (count_2),
  penT=100000/penI=40000 SEPARATE from finals; DB row + 3 txn rows + cost_per_student=140000 all exact.
- Remainder rotation CONFIRMED: institute 100000/3 → 33334-recipient differed across dates (S5 vs S4).
- H7 branch: student branch wins (2), NULL→class fallback (1). Both PASS.
- Edit atomicity: invalid-membership edit → 422 + byte-identical state (fail-fast pre-validation).

## FINDING F1 (real bug): all-absent session → penalty ZERO + invisible + unsettleable
present=0 → shares all-zero → absent_penalty_*=0; NOT in pending (no penalty_only entry);
admin settle → 400 "already settled". The penalty_only flow (teachers.py:737) is UNREACHABLE.
Needs product decision: what base should all-absent penalty use (if any)?

## FINDING F2 (boundary): settle is ADMIN-ONLY (secretary→403) — contradicts "admin/secretary" belief.
Same class as refund. Needs decision.

## FINDING F3 (boundary): teacher invoice→403 (verify_financial_idor: staff+self only) BUT
student_statement of own student→200. Inconsistent teacher financial visibility. Needs decision.

## IDOR wall (all 403 as expected)
submit/grade/hw-grade/details of other class, other-teacher card, approve/reject/suspend,
settle-self, refund, pricing/share, analytics/debtors, bulk-sms/history, update-other-class (own→200).

## Infra lessons
Rate limiters are 5/5min PER-ENDPOINT (login, teacher/student register each). Seed once, persist
tokens+IDs (tokens recoverable from user_sessions). user JWTs survive server restart; slowapi bucket resets.
