# Issue #9 (per-enrollment coverage) + #7 (ledger refund) — IMPL 2026-09-12 (no compile/run)
- P1 models.py:476 ledger TransactionInstallmentAllocation (txn_id, inst_id, amount, created_at) + ix_alloc_transaction/installment; migration = create_all (main.py:101) + CREATE TABLE note for existing DBs.
- P2a finance.py:431 submit scope: linked → [linked.id], unlinked → legacy cross.
- P2b :453 flush (autoflush OFF per models.py:36 — receipts need ids for ledger; commit still single at end) + FIFO state :455-457.
- P2c :491-501 ledger write per successful cover, FIFO across both-split receipts (single-wallet = index 0).
- P3a :906 callback scope (payment.enrollment_id or cross). P3b :918-959 callback loop → conditional-UPDATE + ledger on new_trans (already flushed); 409 rolls back only this payment (single-payment callback; gateway disabled + rewrite TODO noted).
- P3c :903 residual marker: targeted-online + manual-settle write NO ledger (their refunds keep today's behavior) — follow-up.
- P4 :1237-1259 refund reversal from ledger in same DB txn (after total_paid, before audit, inside if-student like total_paid): per-alloc optimistic cond-UPDATE (floor 0, recompute is_paid/paid_at), 3 tries → 409; rows KEPT as history (repeat-refund impossible via is_reversed gate).
- Import :15. Verified: 3× for/else+409 tails intact on disk.
- Step5 trace: E1(I1=500k,I2=500k) E2(I3 older); pay 700k→E1 covers I1 full + I2 200k (2 alloc rows), I3 untouched; refund reverses exactly I1 (500k→0, reopen, paid_at None) + I2 (200k→0); total_paid/wallet same commit.
