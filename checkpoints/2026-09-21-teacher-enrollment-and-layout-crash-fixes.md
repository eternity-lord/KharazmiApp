# تسک جدید — ۴ باگ گزارش‌شدهٔ کاربر: ۲ کرش layout + ۲ مشکل دسترسی معلم

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ · **Branch:** `arena/01a0bf8d-kharazmiapp` · **زبان:** فارسی
- **Base HEAD:** `1e414b6` (CI سبز `35606338318`) · **Tip بعد از این تسک:** `060db6d`
- **کامیت‌ها:** `4ed749b` (layout/کرش‌ها) · `060db6d` (دسترسی معلم/O-22)
- **وضعیت:** هر دو باگ فیکس و تست‌شده؛ اندروید در این محیط کامپایل نمی‌شود ⇒ تأیید نهایی روی دستگاه کاربر

---

## ۱) فهرست باگ‌های گزارش‌شده

| # | گزارش کاربر | نوع | ریشه | فیکس |
|---|---|---|---|---|
| الف | کرش «You must supply a layout_width attribute» در **پروفایل معلم** (Binary XML خط ۴۱۶) | کرش زمان اجرا | ۱ عنصر بدون `layout_width/height` (`activity_teacher_profile.xml`) | `4ed749b` |
| ب | کرش همان پیام در **CRM / سرنخ‌های جذب** (خط ۶۱) | کرش زمان اجرا | ۷ عنصر بدون `layout_width/height` (`activity_crm_leads.xml`) | `4ed749b` |
| پ | معلم بعد از ساخت کلاس نمی‌تواند دانش‌آموز اضافه کند («دسترسی برای انجام این کار ندارید») | ۴۰۳ اشتباه | گیت `check_admin_or_secretary_access` روی `POST /enrollments/add` | `060db6d` |
| ت | معلم نمی‌تواند دانش‌آموز ثبت‌نام (ثبت) کند | ۴۰۳ اشتباه | همان گیت روی `POST /students/register_and_enroll` | `060db6d` |

---

## ۲) باگ الف و ب — جزئیات ریشه و اصلاح

پیام واقعی اندروید هنگام inflate: `Binary XML file line #NN: You must supply a layout_width attribute.`
هر عنصر نمای اندروید باید `layout_width`/`layout_height` داشته باشد یا آن‌ها را از `style` ارث ببرد.

### ریشهٔ دقیق
- `activity_teacher_profile.xml` → کارت «کل بدهی تسویه نشده…» (اطراف خط ۴۱۵) — ۱ عنصر بی‌ابعاد
- `activity_crm_leads.xml` → دکمهٔ «➕ تعریف سرنخ جذب جدید» (اطراف خط ۵۹) — ۷ عنصر بی‌ابعاد
- اسکن کامل: **۳۵ عنصر در ۹ فایل layout** — این موارد **از قبل در `main`** هم بودند (رگرسیون این شاخه نیستند).

### اصلاح (فقط تزریق attribute؛ هیچ جابه‌جایی/تغییر style)
| قاعده | مقدار |
|---|---|
| `layout_width` وقتی parent افقی است | `wrap_content` |
| `layout_width` در غیر این صورت | `match_parent` |
| `layout_height` | `wrap_content` |
| attributeهای موجود | **دست‌نخورده** (مثال: `tvActiveExamTitle` فقط height لازم داشت چون width از قبل بود) |

| فایل | تعداد عنصر |
|---|---|
| `activity_calendar.xml` | ۳ |
| `activity_class_dashboard.xml` | ۲ |
| `activity_crm_leads.xml` | ۷ |
| `activity_exam_list.xml` | ۵ |
| `activity_homework_list.xml` | ۷ |
| `activity_message_list.xml` | ۲ |
| `activity_parent_portal.xml` | ۴ |
| `activity_student_portal.xml` | ۴ |
| `activity_teacher_profile.xml` | ۱ |
| **جمع** | **۳۵ عنصر / ۶۹ سطر افزوده** |

### اعتبارسنجی ایستا (namespace-aware، با احتساب ارث‌بری `style` → `parent`)
| بررسی | نتیجه |
|---|---|
| عناصر بدون `layout_width`/`layout_height` (۹۶ فایل layout) | **۰** |
| attribute تکراری | **۰** |
| XML خراب در کل `res/` (۲۱۲ فایل) | **۰** |
| تفاوت نقاط کرش کاربر (پروفایل معلم ~۴۱۵، CRM ~۵۹) | تأیید شد؛ همان عناصر اصلاح شدند |

> ⚠ در این محیط Gradle/JDK/Android SDK وجود ندارد ⇒ **کامپایل نشده**. تأیید نهایی با نصب روی دستگاه کاربر.

**بازگشت‌های انجام‌شده در مسیر کار (شفافیت):** تلاش اول اسکنر، `parent` استایل را از آیتم‌ها می‌خواند
(نه از attribute روی `<style>`) و ۸ مورد اضافه گزارش می‌کرد (۴۳ به‌جای ۳۵)؛ آن تغییر کامل revert شد و
اسکنر با `provides()` بازگشتی و پارس صحیح `parent` بازنویسی شد. نسخهٔ نهایی: ۳۵ مورد تأییدشده.

---

## ۳) باگ پ و ت — جزئیات ریشه و اصلاح (O-22)

### ریشه
هر دو مسیری که **کلاینت اندروید** برای افزودن/ثبت دانش‌آموز صدا می‌زند، با گیت ادمین/منشی محافظت می‌شدند:

| اندپوینت | فایل | مصرف‌کننده در کلاینت |
|---|---|---|
| `POST /enrollments/add` | `routers/classes.py:373` | `ClassSetupActivity.kt:43` (`students/search_simple` + `enrollments/add`) |
| `POST /students/register_and_enroll` | `routers/students.py:77` | `StudentRegisterActivity.kt:34` |

نتیجه: معلمِ وارد‌شده همیشه `403 {"detail":"شما دسترسی لازم برای این عملیات را ندارید"}` می‌گرفت
⇒ کلاس تازه‌ساخته‌شدهٔ معلم عملاً خالی می‌ماند. (این اولین بار با تست قرمز بازتولید شد — پایین.)

### اصلاح حداقلی (least-privilege)
| مورد | قبل | بعد |
|---|---|---|
| گیت اندپوینت | `check_admin_or_secretary_access` | `check_admin_secretary_or_teacher_access` (جدید در `dependencies.py`) |
| دامنهٔ معلم | — | `resolve_session_teacher(سشن)` + مقایسه با `Course.teacher_id` ⇒ فقط کلاس‌های خودش |
| کلاس معلم دیگر | ۴۰۳ (کل مسیر) | ۴۰۳ **قبل از هر نوشتنی** (پیام گویا) |
| ترتیب بررسی | — | اول ۴۰۴ «ناموجود/آرشیوی» بعد ۴۰۳ «مالِ دیگری» (مثل سیاست H10، دست‌نخورده) |
| شعبه | ازتوکن/درخواست | `resolve_creation_branch(..., teacher=…)` ⇒ **شعبهٔ خودِ معلم** |
| منطق مالی | — | **هیچ تغییری**: همان `Enrollment/Installment/Transaction`، همان کیف‌ها، همان تعرفه |
| گیت تعلیق همکاران | — | عیناً اعمال می‌شود (`InstituteSettings.teachers_active=False` ⇒ ۴۰۳ «متوقف») |
| ادمین / منشی | مجاز | **بدون تغییر** |
| شاگرد / ولی | ۴۰۳ | ۴۰۳ (بدون تغییر) · بدون توکن: ۴۰۱ |

**نکتهٔ امنیتی:** توکن/سشن معلم دوباره اعتبارسنجی می‌شود (`resolve_session_teacher` روی همان سشن،
با fallback سایهٔ کاربر بر اساس موبایل) — معلم نمی‌تواند با دست‌کاری درخواست، `teacher_id` جعل کند.

### آمار تغییر (server)
```
 dependencies.py           | +55   (گیت جدید + resolve_session_teacher)
 routers/classes.py        | +23/-? (enrollments/add: گیت + مالکیت کلاس)
 routers/students.py       | +24/-? (register_and_enroll: گیت + مالکیت کلاس + شعبه)
 tests/test_teacher_student_enrollment_access.py | +219 (جدید، ۸ تست)
 ۵ فایل تست موجود: ۱۴ فراخوانی مستقیم `_=` → `sub_role=`
```

---

## ۴) تست — چرخهٔ قرمز → سبز (طبق قانون پروژه)

### تست جدید: `Kharazmi_Server/tests/test_teacher_student_enrollment_access.py`
دنیای درون‌حافظه‌ای: شعبه ۱ · ادمین · منشی · دو معلم (کلاس ۷۱ مال معلم۱، کلاس ۷۲ مال معلم۲،
کلاس ۷۳ آرشیوی مال معلم۱) · یک دانش‌آموز. شش سشن توکن‌دار.

| # | سناریو | انتظار | قبل از فیکس | بعد از فیکس |
|---|---|---|---|---|
| ۱ | معلم۱ دانش‌آموز موجود را به کلاس ۷۱ خودش اضافه کند | ۲۰۰ + رکورد با شعبهٔ ۱ | ❌ ۴۰۳ | ✅ |
| ۲ | معلم۱ در کلاس ۷۲ (معلم دیگر) | ۴۰۳ بدون نوشتن | ✅ | ✅ |
| ۳ | کلاس آرشیوی ۷۳ توسط مالکش | ۴۰۴ | ❌ ۴۰۳ | ✅ |
| ۴ | ثبت دانش‌آموز جدید توسط معلم۱ برای کلاس ۷۱ | ۲۰۰ + شاگرد + ثبت‌نام | ❌ ۴۰۳ | ✅ |
| ۵ | ثبت دانش‌آموز جدید برای کلاس معلم دیگر | ۴۰۳ + بدون شاگرد جدید | ✅ | ✅ |
| ۶ | ادمین ۲۰۰ · منشی تکراری ۴۰۰ · شاگرد/ولی ۴۰۳ · بدون توکن ۴۰۱ | — | ✅ | ✅ |
| ۷ | بی‌اثری مالی: پرداخت اولیه ۴۰۰٫۰۰۰ ⇒ کیف آموزشگاه/موجودی + `Transaction(enrollment_payment, institute)` | مثل مسیر ادمین | ❌ ۴۰۳ | ✅ |
| ۸ | گیت تعلیق همکاران (`teachers_active=False`) | ۴۰۳ حاوی «متوقف» | ❌ ۴۰۳ (پیام اشتباه) | ✅ |

**خروجی قرمز (قبل از فیکس):** `5 failed, 3 passed` — هر ۵ شکست با پیام دقیق گزارش‌شدهٔ کاربر:
```
AssertionError: 403 != 200 : {"detail":"شما دسترسی لازم برای این عملیات را ندارید"}
```
**خروجی سبز (بعد از فیکس):** `8 passed`

### تست‌های قفل‌کنندهٔ باگ که آگاهانه به رفتار درست به‌روز شدند
| فایل | تغییر | دلیل |
|---|---|---|
| `test_branch_resolution.py` | ۸ فراخوانی مستقیم `_="admin"/"secretary"` → `sub_role=…` | تابع با DI واقعی صدا زده می‌شود؛ نام پارامتر گیت عوض شده |
| `test_classes.py` | ۲ | همان |
| `test_priority2_financial.py` | ۱ | همان |
| `test_priority3_integrity.py` | ۱ | همان |
| `test_tuition_installments_audit.py` | ۲ | همان |
| `test_security_audit.py` | گیت جدید به فهرست گاردهای معتبر افزوده شد | گارد فهرست نام‌سخت‌کد داشت و اندپوینت‌های محافظت‌شدهٔ جدید را «بدون محافظ» می‌دید |

### نتیجهٔ سوئیت کامل
| مرحله | نتیجه |
|---|---|
| مبنا (`1e414b6`) | ۱۰۶۹ passed |
| بعد از فیکس ناقص (بدون به‌روزرسانی تست‌های مستقیم) | ۲۴ failed, 1053 passed ⇒ کشف شد و رفع شد |
| **بعد از فیکس کامل (`060db6d`)** | **۱۰۷۷ passed** (۱۰۶۹ + ۸ جدید) · زمان ۱۱۳ ثانیه |
| `import main` روی کپی `/tmp/impchk` | OK — ۲۱۱ route (شامل هر دو اندپوینت) |
| `md5sum gaj_db.db` قبل/بعد | `f048f8d118b33c4eaa944490594121d7` (دست‌نخورده ✅) |

دستور اجرا:
```bash
env -u PYTHONPATH DATABASE_URL=sqlite:////tmp/<n>.db JWT_SECRET_KEY=<hex> \
  /home/user/.venv/bin/python -m pytest -q . -p no:randomly
```

---

## ۵) چه چیزی تست نشده / محدودیت‌ها
1. **کامپایل اندروید انجام نشد** (Gradle/JDK/Android SDK در محیط نیست) — فقط اعتبارسنجی ایستای XML.
2. اجرای واقعی روی **دستگاه کاربر** برای هر ۴ باگ باقی است.
3. `Kharazmi_Server/tests/*.py` فقط با `sqlite` موقت تست شده؛ روی DB واقعی هیچ نوشتنی انجام نشد.

## ۶) قدم بعدی
کاربر تست دستی: الف) باز کردن پروفایل معلم و CRM بدون کرش · ب) ساخت کلاس توسط معلم و
افزودن/ثبت دانش‌آموز در آن کلاس. سپس باگ‌های جدید را گزارش می‌کند.
