# L5 Batch 2 — Attendance + Main (2026-09-13, no Android compile)
- strings.xml +109 (6 common_*, ~60 attendance_*, ~40 main_*); XML valid. Reused: error_server, btn_retry, day_* x7, month_* x12, classes_students.
- AttendanceActivity.kt: 70 replacements. Extra find: """ HTML receipt block (616-664) with 11 Persian fragments -> ${getString()} micro-labels (HTML stays in code). Nested templates (277/356/553/554/556/559/688/729/731/741) -> format strings with preserved code args.
- MainActivity.kt: 64 replacements. Weekday/month whens -> existing day_*/month_* resources. Menu/register dialogs index-based (safe).
- Logic-map audit: NO API/logic-mapped Persian in either file (all conditions English/enum/int; lastError display-only; options dialogs use `which` index). Nothing set aside.
- Deliberate keeps: "؛ " join separator (:218, punctuation like batch-1 "،"); String.format(Locale.US,"%,d") (no Persian); // comments.
- Verify: zero user-facing Persian literals in both files; all R.string refs exist. Script ordering note: simple-before-complex double-hit 2 nested inners; outers fixed after (559/356) - final state verified correct.
