# فاز دیباگ — سناریوی end-to-end #۶: گزارش‌ها، خروجی‌ها و تحلیل‌ها

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ · **Branch:** `arena/01a0bf8d-kharazmiapp`
- **Base HEAD:** `57df71c` (سناریوی ۵)
- **فایل تست:** `Kharazmi_Server/tests/test_e2e_reports_exports_analytics.py` (۱۷ تست)
- **قاعدهٔ فاز:** صفر تغییر در کد برنامه.

## ۱) زنجیرهٔ سنجیده‌شده

| خروجی | اندروید | سرور |
|---|---|---|
| خلاصهٔ مالی | `ReportActivity.kt:70` → `reports/financial_summary` | ماهانه/سالانه institute و teacher |
| بدهکاران | `ReportActivity` → `reports/debtors` | لیست با مبلغ بدهی واقعی |
| بدهکاران اکسل | `reports/debtors/excel` | `openpyxl` + شیت راست‌به‌چپ |
| خروجی‌های ادمین | `ApiInterfaces.kt:134-142` (`@Streaming`) → `exports/debtors` · `exports/overdue_installments` · `exports/audit_alerts` | CSV با BOM + محافظت تزریق فرمول |
| نمودارها | `ChartActivity.kt:75` → `reports/chart-data` | income/student/attendance/shares |
| KPI | `AdminDashboardActivity.kt:310` → `dashboard/kpis` | `dashboard.py:61` (کش ۶۰ ثانیه) |
| صورت‌حساب شاگرد | `reports/student_statement` + `/print` (HTML) | اعداد واقعی + کارت آموزشگاه |
| لاگ ممیزی | `ApiInterfaces.kt:150-164` → `audit-trail/logs` | ادمین‌فقط + اعتبارسنجی پارامترها |
| هشدارهای مشکوک | `ApiInterfaces.kt:96` → `audit/suspicious_patterns` | ادمین‌فقط |
| یادآوری معوقات | `ApiInterfaces.kt:112-115` → `dunning/drafts` · `dunning/send_batch` | دسته‌بندی بحرانی/معوق/نزدیک + پیامک |

## ۲) نتیجهٔ تست‌ها

| سنجش | نتیجه |
|---|---|
| فایل سناریوی ۶ | ✅ **17 passed** |
| کل سوئیت (پایان همهٔ سناریوها) | ✅ **1038 passed** (94.42s) |
| md5 دیتابیس واقعی | ✅ بدون تغییر `f048f8d118b33c4eaa944490594121d7` |

## ۳) 🐞 یافته‌ها

### یافتهٔ ۶-الف — نمودار درآمد، دادهٔ ساختگی است — **بالا**

`routers/reports.py:127-133`:

```python
income_data = [
    {"day": "شنبه",     "amount": total_income // 5},
    {"day": "یکشنبه",   "amount": total_income // 4},
    {"day": "دوشنبه",   "amount": total_income // 3},
    {"day": "سه‌شنبه",  "amount": total_income // 2},
    {"day": "چهارشنبه", "amount": total_income},
]
```

هیچ‌کدام از این پنج عدد از داده محاسبه نشده‌اند: تقسیم‌های دلبخواه `total//5 .. total` هستند.
**جمع ستون‌ها ۲.۲۸ برابر درآمد واقعی است** و در اپ به‌عنوان «درآمد هفتگی» نمایش داده می‌شود.
تست سند: `test_12_chart_data_income_chart_is_fabricated_bug`.

### یافتهٔ ۶-ب — «درآمد» جمعِ علامت‌دارِ همهٔ تراکنش‌هاست (بدون فیلتر نوع/کیف پول) — **بالا**

هر دو مسیر درآمد (`routers/reports.py:124` برای نمودار و `routers/dashboard.py` `_today_revenue_sql`
برای KPI) همهٔ تراکنش‌های بازه را جمع می‌زنند:

- **شارژ جلسهٔ کلاس** (که بدهیِ شاگرد است، نه خروج پول) از درآمد **کم** می‌شود:
  ۵۰۰٬۰۰۰ پرداخت − ۲۰۰٬۰۰۰ شارژ جلسه ⇒ «درآمد امروز = ۳۰۰٬۰۰۰» (کم‌گویی ۲۰۰٬۰۰۰).
- **پرداختی که به کیف پول معلم رفته** هم به‌عنوان درآمد آموزشگاه شمرده می‌شود (بیش‌گویی).

تست سند: `test_14_kpis_are_consistent_with_database` (KPI) و `test_12` (نمودار).

### یافتهٔ ۶-ج — پرداخت‌های بی‌تاریخ در «وصول‌شدهٔ ماه» نمی‌آیند — **کم/متوسط**

`financial_summary` فقط ردیف‌های دارای تاریخ را در بازهٔ ماه می‌شمارد؛ ردیف بدون تاریخ
(که فیکس A3 وجودش را تأیید کرده) از جمع ماه حذف می‌شود، در حالی که همان مبلغ در
«صورت‌حساب شاگرد» و «بدهکاری» دیده می‌شود ⇒ دو عدد ناهم‌خوان در دو صفحهٔ اپ.
(سنجش دیتابیس فعلی آموزشگاه: صفر ردیف بی‌تاریخ ⇒ فعلاً اثر عملی ندارد.)
تست سند: `test_1` و `test_4`.

## ۴) رفتارهای تأییدشدهٔ درست

- لیست بدهکاران: فقط بدهکار واقعی، مبلغ = شهریه − پرداخت‌شده؛ شاگرد بی‌بدهی حذف ✅
- **خروجی‌های CSV:** BOM یونیکد (اکسل فارسی را درست نشان می‌دهد)، `attachment`،
  محافظت از تزریق فرمول (`=`, `+`, `-`, `@` با `'` بی‌اثر می‌شوند) ✅
- خروجی معوقات: فقط قسط سررسیدگذشتهٔ پرداخت‌نشده؛ پرداخت‌شده و آینده حذف؛ وضعیت «بحرانی» ✅
- اکسل بدهکاران: فایل واقعی `openpyxl`، شیت راست‌به‌چپ، سرستون‌های فارسی ✅
- فیلتر شعبه روی خروجی‌ها ✅
- صورت‌حساب شاگرد و برگهٔ چاپی: نام/بدهی/کارت آموزشگاه واقعی و بدون `None` ✅
- **سهم معلم/آموزشگاه فقط برای ادمین:** برای منشی `shares_chart=None` (سیاست A1) ✅
- لاگ ممیزی: ادمین‌فقط (منشی/شاگرد ۴۰۳)، `entity_type` نامعتبر ۴۰۰، سه تراکنش seed در لاگ ✅
- هشدارهای مشکوک (`/audit/suspicious_patterns`): ادمین‌فقط ✅
- پیش‌نویس‌های یادآوری: فقط قسط معوق، دسته‌بندی درست (بحرانی/معوق/نزدیک)، نیاز به موبایل ولی،
  متن پیشنهادی آماده؛ `send_batch` پیامک می‌سازد و لیست خالی ۴۰۰ می‌گیرد ✅
- KPI: قرارداد شش‌کلیدی؛ `overdue_installments_count=1`، `total_overdue_amount=300,000`،
  `active_students_count=3` درست ✅

## ۵) Commit و Push

- **Commit message:** `test: add end-to-end report, export and analytics coverage`
- **پایان سناریوهای ۲–۶.** گزارش نهایی تجمیعی در `checkpoints/2026-09-21-e2e-debug-final-report.md`.
