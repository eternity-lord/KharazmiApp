# Checkpoint — تست E2E واقعی بک‌اند (فقط سرور، بدون کامپایل اندروید)

Date: 2026-09-12. Report-only؛ هیچ فیکسی اعمال نشد.

## قدم ۱ — آماده‌سازی
- کپی: `gaj_db.db` ← `/tmp/gaj_e2e.db` (هش اول cc487ee9...، مطابق اصل).
- `pip install -r requirements.txt` انجام شد (fastapi/uvicorn/sqlalchemy/slowapi/...).
- ستاپ روی کپی (نه اصل): ادمین `admin` با hash_password خود اپ؛ parent_mobile هر دو
  شاگرد = `09350000000` (مسیر چندفرزندی)؛ بعداً branch_id=1 برای شاگردان/کلاس/ثبت‌نام‌ها.
- سرور واقعی: `DATABASE_URL=sqlite:////tmp/gaj_e2e.db uvicorn main:app :8123` (env روی
  .env سوار می‌شود چون load_dotenv اوررایت نمی‌کند). Auto-patch/main روی کپی اجرا شد.

## قدم ۲ — نتایج (۲۴ چک: ۲۴ موفق، ۰ خطای واقعی)
1. لاگین ۴ نقش ✅: ادمین (200)؛ معلم جدید (register→403 پیش‌ازتأیید→approve دستی→200)؛
   شاگرد (OTP از sms_logs کپی ← لاگین 200)؛ ولی (OTP←multi-children+temp_token←select_child←سشن کامل + child_profile 200).
2. پرداخت ✅: `/finance/pay` مبلغ 500000 به institute: total_paid ‏2000000←2500000،
   wallet_institute هم‌مقدار، wallet_balance سازگار، تراکنش deposit با enrollment_id/course_id/branch_id.
3. جلسه ✅ (بعد از ست شعبه): حاضر+غایب غیرموجه ← SessionLog (معلم 500000 + آموزشگاه
   50000 + جریمه غیبت 500000/50000)، ۲ Attendance، ۲ session_charge هرکدام ‎-550000‎،
   کیف‌ها دقیق کم شدند (کیف منفی مجاز است — رفتار مشاهده‌شده، نه قضاوت).
   تلاش اول بدون شعبه 400 تمیز داد و هیچ ردیف نیمه‌کاره‌ای نگذاشت (اتمیک‌بودن تأیید شد).
4. تسویه ✅: settlement_id=1 مبلغ 1000000 (= قراردادی 500000 + جریمه غیبت 500000،
   طبق H6-A2/H8) + تراکنش settlement_payout به‌مبلغ ‎-1000000‎؛ تسویه مجدد 400 «قبلاً
   تسویه شده‌اند». نکته: is_billed فقط روی Present/Late می‌نشیند (by-design طبق کامنت
   teachers.py ~752؛ غایب 0 ماند ولی جریمه‌اش پرداخت شد).
5. منفی ✅: `/teachers/list` و `/finance/pay` با توکن شاگرد هر دو 403 (C1).
- لاگ سرور: صفر Traceback و صفر 500 در کل ران.

## قدم ۴ — پاکسازی
- سرور stop شد؛ `/tmp/gaj_e2e.db` و اسکریپت‌ها حذف شدند؛ هش اصل همچنان cc487ee9
  (سایز/mtime دست‌نخورده). اندروید کامپایل نشد.
