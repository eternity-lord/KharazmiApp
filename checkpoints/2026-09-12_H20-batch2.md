# Checkpoint — H20 batch 2: reads (norm-only lookups, raw-OR-norm proofs)

Date: 2026-09-12. auth.py + parent.py, 14 ops asserted + visual read-back.
No compile/run.

## Lookups (norm-only per order; None -> same errors, never new ones)
- login_user: _raw_login/login_mobile/_throttle_mobile (:28-31); admin query
  raw-fallback (usernames like "admin" aren't mobiles) (:39); teacher query
  norm-only + explicit None->miss (:71-72) -> same tail 404.
- request_student_otp / student_login / request_parent_otp / parent_login:
  canonical `mobile` var (queries + OTP/rate keys + store unified); None ->
  each flow's own same-error (request: same 404; login: same OTP-400, i.e.
  what unknown numbers get today). Temp-JWT mobile now canonical.
- select_child IDOR (:~211): canonical both sides; either-side-None ->
  exact-compare fallback (today's behavior, no new lockout, no None==None pass).

## Proofs (raw-OR-canonical — deliberate exception, zero attacker gain)
change_password both branches (:289/:312), change_mobile current (:635):
match if raw-equal OR norm-equal. Password/session still required, so the OR
can't be abused; lookups stay norm-only because they need ONE key.

## L2: batch-1 K block REWRITTEN (pair-consistent shadow)
Batch-1 K had a live takeover flaw (canonical-first lookup could return
ANOTHER teacher's shadow in true-duplicate collisions) + one-sided heal
broke username==mobile invariant. New (:~92-116): legacy-exact lookup first,
pair-heal (username+mobile) only if canonical free of Teacher-owner and
User-clash, adopt-canonical only if unowned, create with consistent pair.
True duplicates stay legacy-consistent (login works, backfill flags them).

## Step-4 resolvers: NO CHANGE (proven unnecessary)
get_me (:363), classes.py:934, reports.py:403 compare stored-vs-stored under
the preserved invariant — exact match works for canonical AND legacy pairs.
FOUND-ADJACENT (not fixed, pre-existing): update_teacher never syncs shadow
username (unlike change_mobile/admin-creds) -> diverges pairs on its own;
batch-3 candidate.

## HEADLINE: true blast radius of norm-only (accepted risk, but wider than stated)
Not "some legacy users with mismatched input" — EVERY legacy row misses with
EVERY input (any input normalizes-away or Nones; canonical query never equals
legacy bytes). Legacy teacher/student/parent = effectively locked out until
batch 3. Admin "admin" unaffected (raw fallback). Proofs unaffected (OR).
Direction is fail-closed (safe) but deployment MUST pair batch 2 with batch-3
backfill (or accept the lockout window). Raw-fallback on lookups exists as an
escape hatch (harmless: password/OTP still gate) if deployment can't wait —
not applied per explicit norm-only order.
Step-5 canonical trace: stored "0912..." + input "+98912..." -> normalized ->
match -> success; throttle/OTP buckets unified.
