# L14 batch R1 — 4 RED endpoints guarded (2026-09-14, no compile/run)

## Step 1 — finance.py:1753-1757: installment reminder → check_admin_or_secretary_access
- Was: any login (even a student) could trigger a REAL SMS to ANY student's parent + SmsLog row.
- Now: admin/secretary only. finance.py already imported the guard (line 20). + FIX comment.

## Step 2 — branches.py: resources → check_admin_or_secretary_access ×3
- :10 import extended with check_admin_or_secretary_access.
- POST /resources (create, guard :130), PUT /resources/{id} (update, guard :155),
  POST /resources/bookings (book, guard :188). + FIX comments each.
- Untouched: GET /branches (:116), GET /resources (:175) reads stay login-only (🟢);
  GET /dashboard/branch_stats (:234) keeps its own in-body 403.

## Step 3 — Legit-path verification (nothing breaks)
- Android app: ZERO callers of /finance/installments/*/remind and /resources* (grep; only
  false+ like `context.resources`, `displayMetrics`).
- Parent portal (parent.py): no references. Server-internal: no callers (only the defs +
  SmsLog target string + unrelated automation action names).
- Conclusion: all 4 are currently uncalled endpoints → guard cannot break any existing flow.
  Intended users (admin AND secretary) explicitly preserved by the chosen guard; only
  teacher/student/parent blocked, none of which has any UI path here.

## Verify: ast.parse both files (static only) + grep line numbers.
