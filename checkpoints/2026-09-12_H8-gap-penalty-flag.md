# Checkpoint — بستن گپ H8: تسویه‌ی جلسه‌ی تماماً-غایب (is_penalty_settled)

Date: 2026-09-12. Asserted + read-back. No compile/run.

## قدم ۱ — فیلد مستقل (models.py:335 + main.py:264)
- `SessionLog.is_penalty_settled` (Boolean، دیفالت False) — سطح-جلسه، مستقل از Attendance.
- migration در انتهای لیست patches (الگوی H6/M13): `BOOLEAN DEFAULT FALSE` + بک‌فیل
  یک‌باره با CASE: جلسه‌ای که پیش‌ازاین فیکس تسویه شده (ردیف Present/Late با billed دارد)
  جریمه‌اش هم همان‌موقع پرداخت شده ← TRUE؛ بقیه FALSE. فقط هنگام ADD COLUMN اجرا می‌شود
  (حلقه‌ی main.py) پس دابل‌پیِ retro رخ نمی‌دهد.

## قدم ۲ — گیت P1 دوشاخه (teachers.py:751-766)
- شاخه‌ی قدیمی (unbilled Present/Late) سر جایش؛ شاخه‌ی جدید `penalty_open_ids` (:762):
  `penalty>0 و فلگ False`. جلسه با حداقل یک بخشِ باز قابل انتخاب است؛ کاملاً-تسویه‌شده
  (هر دو بسته) همان 400 قبلی را می‌گیرد. چک `if not attendances` به
  `if not attendances and not penalty_open_ids` (:782) — وگرنه تماماً-غایب همین‌جا گیر می‌کرد.

## قدم ۳ — مبلغ (teachers.py:786-789، فرمول بدون تغییر)
- فرمول سطح-جلسه از قبل `contract + penalty` بود پس تماماً-غایب خودکار پوشش دارد
  (contract صفر)؛ فقط کامنت دقیق شد. فلیپ فلگ در P4b پایین انجام می‌شود.

## قدم ۴ — ضد double-pay (teachers.py:807-816 + attendance.py:730)
- P4b: آپدیت مشروط فلگ با چک rowcount (فلسفه‌ی H8-P4) — روی مسیر تماماً-غایب گارد P4
  خلأ است (۰=۰) پس بدون این، ریس موازی جریمه را دوباره می‌پرداخت؛ بازنده rollback + همان 409.
- جلسه‌ی مختلطِ تسویه‌شده: حضور بسته (billed) + جریمه بسته (فلگ) ← از هیچ شاخه‌ای رد نمی‌شود.
- همراهِ ضروری (خارج از ۵ قدم ولی لازم برای قدم ۴): قفل ویرایش جلسه (`attendance.py:730`)
  با `or session.is_penalty_settled` — وگرنه جلسه‌ی تماماً-غایبِ تسویه‌شده (بدون ردیف billed)
  بعد از تسویه قابل دست‌کاری و تسویه‌ی مجدد بود (recalc در :799).

## قدم ۵ — ردیابی دستی (جلسه‌ی تماماً-غایب، جریمه‌ی ۲۰۰٬۰۰۰)
- قبل: unbilled={} ← 400 «قبلاً تسویه شده‌اند»؛ هیچ‌وقت قابل تسویه نبود.
- بعد، تسویه‌ی اول: penalty_open={S} ← گیت پاس؛ attendances=[] ولی penalty_open ناتهی ← بدون
  خطا؛ total = 0 + 200000؛ P4 با ۰=۰ پاس؛ P4b فلگ را TRUE می‌کند؛ payout ‏-200000‏ + رکورد Settlement.
- تسویه‌ی دوم: unbilled={} و penalty_open={} (فلگ TRUE) ← 400. دقیقاً یک‌بار. ✅

## یادداشت follow-up (بدون تغییر، خارج اسکوپ)
- هر ۶ گزارش طلب (pending و...) هنوز فقط جلسات دارای Present/Late تسویه‌نشده را نشان
  می‌دهند؛ جلسه‌ی تماماً-غایبِ قابل‌تسویه با ID صریح settle می‌شود ولی در لیست pending
  دیده نمی‌شود (گپ discovery، نه double-pay). اگر خواستی، گزارش‌ها را هم به‌روزرسانی می‌کنم.
