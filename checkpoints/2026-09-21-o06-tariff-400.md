# O-06 — نبود تعرفه ⇒ ۵۰۰ (خطای سرور) به‌جای ۴۰۰ (تنظیمات ناقص)

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ · **Branch:** `arena/01a0bf8d-kharazmiapp` · **موج:** ۱
- **Base HEAD:** `306a4e7` (O-05)
- **دسته:** سرور/UX · **شدت:** متوسط

## بازتولید (قرمز اول)
`test_8a`/`test_8b` سناریو ۲ به انتظار **۴۰۰** تغییر کردند ⇒ هر دو **قرمز** شدند (پاسخ واقعی: ۵۰۰).

## بررسی تصمیم H5 قبل از تغییر (طبق دستور)
سند `checkpoints/2026-09-12_L-batch3-carryovers.md` (قدم ۲ + گارد H5):

> «نبودن ردیف تعرفه … ← ۵۰۰ صریح با پیام «جدول تعرفه را ثبت کنید» … دیگر `T_total=0` ساکت
> (جلسهٔ مجانی) نداریم.»

⇒ **دلیلِ محکم برای «خطای واضح» وجود دارد، اما نه برای کد ۵۰۰.** فلسفهٔ H5 («خطا بده، ساکت ادامه نده»)
عیناً حفظ شد؛ فقط کد وضعیت به چیزی که واقعاً هست (خطای تنظیمات/درخواست) اصلاح شد.
**Seed اضافه نشد** (طبق دستور).

## فیکس (حداقلی)
`Kharazmi_Server/financial_calculations.py` → هر ۴ مورد `status_code=500` ⇒ `status_code=400`
با همان `detail` و کامنت مرجع O-06/H5 (خطوط پیش از تغییر: ۲۷۷، ۲۸۸، ۳۴۰، ۳۵۵).

## تست
- سناریو ۲: **10 passed** (شامل `test_8a_missing_institute_share_is_a_clear_4xx` و `test_8b`).
- کل سوئیت: **1038 passed** · md5 دیتابیس واقعی بی‌تغییر.
- `ast.parse` + `from main import app` واقعی روی کپی `/tmp`: سالم.

## Commit / Push
- پیام: `fix: return 400 instead of 500 for missing tariff settings (O-06)`
