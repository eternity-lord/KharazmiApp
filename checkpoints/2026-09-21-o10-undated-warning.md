# O-10 — پولِ بی‌تاریخ در گزارش‌های مالی دیده نمی‌شد

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ · **Branch:** `arena/01a0bf8d-kharazmiapp` · **موج:** ۲ (مورد ۹)
- **Base HEAD:** `b6f9746` (O-09) · **دسته:** مالی/گزارش · **شدت:** متوسط

## ریشه
سیاست A3/E1 درست است: ردیف بی‌تاریخ حذف نمی‌شود و تاریخ جعلی هم نمی‌گیرد. ولی در عمل این ردیف‌ها
به هیچ بازهٔ ماه/سالی نسبت داده نمی‌شوند ⇒ در `collected` ماهانه/سالانه **هرگز نمی‌آمدند** و هیچ
فیلدی هم در پاسخ نبود که به مدیر بگوید چنین پولی وجود دارد. نتیجه: پولی که واقعاً به حساب آمده،
در گزارش مالی «گم» به نظر می‌رسید.

## اعداد قبل/بعد روی کپی `/tmp/wave2_o10.db` (کپی `gaj_db.db` با md5 یکسان)
| سناریو | قبل (بدون فیلد) | بعد |
|---|---|---|
| کپی دست‌نخوردهٔ دیتابیس توسعه | `collected` ماهانه = 0 — هیچ راهی برای دیدن پول بی‌تاریخ نبود | `undated_count=0`, `undated_amount=0` |
| کپی + ۲ واریزی بی‌تاریخ/نامعتبر (۲۵۰٬۰۰۰ + ۱۲۰٬۰۰۰) و ۱ واریزی تاریخ‌دار امروز (۵۰۰٬۰۰۰) | `collected` = 500,000 (فقط تاریخ‌دار) و ۳۷۰٬۰۰۰ تومان **باز هم پنهان** | `collected` = 500,000 (دست‌نخورده) + `undated_count=2`, `undated_amount=370,000` |

## فیکس
| فایل | تغییر |
|---|---|
| `financial_calculations.py` | `count_undated_payments(db, target_wallet, branch_id, course_ids)` ⇒ `(تعداد، مبلغ)` پرداخت‌های مثبتِ بی‌تاریخ |
| `routers/reports.py` (`get_financial_summary`) | `undated_count`/`undated_amount` در پاسخ هر دو نوع کاربر (مؤسسه: کیف آموزشگاه با فیلتر شعبه؛ معلم: کیف معلم، فقط کلاس‌های خودش + پرداخت‌های عمومی) — **جمع‌های فعلی دست‌نخورده** |
| `ReportActivity.kt` | فیلدهای nullable در مدل + خط هشدار |
| `activity_report.xml` | `tvUndatedWarning` (پیش‌فرض `gone`، در `ButtonAnimator` چیزی تغییر نکرد) |
| `strings.xml` | `rpt_undated_warning` = «%1$s پرداخت بی‌تاریخ (%2$s تومان) در جمع‌های بالا نیامده است» |

⚠️ کد Kotlin/XML کامپایل نشده (JDK/Gradle در سندباکس نیست)؛ XML با parser معتبرسازی شد.

## تست
- **قرمز اول:** `test_14c_financial_summary_surfaces_undated_payments` ⇒ `KeyError: 'undated_count'`.
- پس از فیکس: سناریو ۶: **19 passed** · کل سوئیت: **1053 passed** · md5 دیتابیس واقعی بی‌تغییر.
- `test_1` سناریو ۶ (که «پول بی‌تاریخ در `collected` نمی‌آید» را قفل می‌کند) بدون تغییر سبز ماند ⇒ جمع‌ها دست‌نخورده.

## Commit / Push
- پیام: `feat: surface undated payments in the financial summary (O-10)`
