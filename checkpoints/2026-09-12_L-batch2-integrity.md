# Checkpoint — L بچ ۲: یکپارچگی داده (L2/L7/L11/L13)

Date: 2026-09-12. Asserted + read-back per step. No compile/run.

## قدم ۱ — L2: چرخش عادلانه‌ی باقیمانده (financial_calculations.py + attendance.py)
- هلپر `_distribute_remainder` (:188): آفست شروع از sha256 پایدار روی
  seed جلسه (نه hash() پروسه‌ای، نه رندوم)؛ دقیقاً remainder تا ‎+۱‎ پس
  T_total/I_total بیدرنگ حفظ می‌شود. salt جدا برای سهم معلم/آموزشگاه تا
  چرخش‌هایشان مستقل باشد. بدون seed = رفتار legacy (idxهای اول).
- seed در هر دو کال‌سایت (`{course.id}:{data.date}` در :396/:780): شناسه‌ی
  جلسه هنوز ساخته نشده (compute قبل از insert است) ولی (کلاس،تاریخ) با ایندکس
  یکتای Bug 17 عملاً جلسه‌ی فعال را یکتا می‌کند — هر جلسه آفست متفاوت.
- جمع‌ها: تغییری نکرد (همان base/remainder؛ فقط جای +۱ها چرخید).

## قدم ۲ — L7: paid_at شمسی (finance.py:164 + ۵ سایت)
- تصمیم: تبدیل به شمسی قبل از ذخیره (نه برچسب میلادی) — چون Transaction.date
  رشته‌ی شمسی کلاینت است و paid_at مستقیم به آن می‌رود (:1428) و due_date هم
  شمسی است؛ میلادی‌ماندن یعنی دو تقویم در یک ردیف/رسید.
- هلپر `_jalali_now_str` (jalali_date_string مبدل H3 + ساعت) + ۵ سایت:
  تسویه‌ی خودکار :424، آنلاین :811/:833، فلیپ ادمین :1291، دستی :1378.
  خواننده‌ها نمایشی‌اند (passthrough) پس امن. ردیف‌های تاریخی میلادی می‌مانند
  (parse_project_date هر دو را درست می‌خواند) — بدون بک‌فیل.
- یافته‌ی مجاور (فیکس نشده): حلقه‌ی آنلاین :825-835 همان الگوی قدیمی
  «فقط پوشش کامل» است و paid_amount هم نمی‌زند (ناقض invariant تغییری M13) —
  کاندیدای بچ بعدی.

## قدم ۳ — L11: version اجباری (students.py:447 + teachers.py:417)
- دو اندپوینت: update_student و update_teacher (همان H1/C11). version==None ←
  400 صریح (اسکیما Optional ماند تا 422 نگیریم). اپ هر دو را می‌فرستد
  (EditStudent:317، EditTeacher:147) پس سازگار. 409 و bump دست‌نخورده.

## قدم ۴ — L13: مقطع با لیست ثابت (financial_calculations.py:219-260)
- تطبیق دقیق بعد از نرمال‌سازی (strip + ي/ك + نیم‌فاصله): اول..ششم/ابتدایی/
  دبستان→elem؛ هفتم..نهم/راهنمایی/«متوسطه اول»→mid؛ دهم..دوازدهم/دبیرستان/
  «متوسطه دوم»→high. ناشناخته‌ها («متوسطه»ی تنها، کنکور، سایر، نامشخص — هر سه
  تای آخر واقعاً از اپ می‌آیند) ← None.
- None فقط وقتی خطای 400 می‌دهد که fallback تعرفه واقعاً لازم باشد (کلاس
  بدون نرخ)؛ کلاسِ دارای نرخ بی‌نیاز از مقطع ادامه می‌دهد. پیش‌فرض حدسی
  high_school حذف شد (None). category مصرف‌کننده‌ی خارجی نداشت پس امن.
- یافته‌ی مجاور (فیکس نشده): نبودن ردیف PricingTable ← T_total=0 ساکت (برخلاف
  InstituteShare که 500 می‌دهد) — کاندیدای بچ بعدی.
