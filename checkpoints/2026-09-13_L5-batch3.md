# L5 Batch 3 — Invoice + StudentProfile (2026-09-13, no Android compile)
- strings.xml +124 (1 common_unknown_class shared by both files, ~62 invoice_*, ~60 profile_*); XML valid. Reused: common_thanks, attendance_toman.
- InvoiceActivity.kt: 66 replacements. H16/idempotency logic PROVEN intact: formSig :500 byte-identical, payment_method :507, res.duplicate :533 + 404-branch :582-585 conditions untouched (only messages -> resources), isModified ternary :714 intact (only stamp literal moved, HTML-escaped in xml).
- SET ASIDE (logic-mapped, per rule): :261 "کد: " substring parser (producer = server client-class strings); :425-427/:431 payMethod when (values feed API payment_method :507 + idempotency formSig :500) + :430 Log line. Same fragility class as batch-1 discount trio.
- StudentProfileActivity.kt: 62 replacements. Audit clean: no """, no Persian ==, photo-options dialog index-based.
- Extra finds: confirmation """ block :486-491 (3 Persian lines incl. missed :487 with nested format); HTML receipt """ :694-731 (9 fragments -> micro-labels).
- Verify: zero remaining user-facing literals except the 5 set-aside lines; all R.string refs exist.
- Script notes: (1) simp-list tuple typo crashed pre-write (nothing written, reran fixed); (2) tool-result JSON escaping mimicked backslashes — actual """ content uses plain quotes.
