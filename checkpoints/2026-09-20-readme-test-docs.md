# نوبت ۷ — تصمیم B3: مستندسازی دستور اجرای تست‌ها در `README.md`

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ (نوبت ۷ از برنامهٔ «۲۶ تصمیم باز»)
- **Branch:** `arena/01a0bf8d-kharazmiapp`
- **Base HEAD:** `338199973cd9f1ce2715c26a70c1965d17221fe9` (پایان نوبت ۶ / B2)
- **سند نقشه:** `checkpoints/2026-09-20-open-decisions.md` — تصمیم B3
- **دامنه:** فقط `README.md`. صفر تغییر در کد.

---

## ۱) مشکل و ریشه

`README.md` فقط یک خط بود (`# KharazmiApp`) — یعنی برای هر همکار جدید (یا خودمان بعد از چند ماه):

- معلوم نبود تست‌ها کجا هستند و چطور اجرا می‌شوند؛
- معلوم نبود `DATABASE_URL` و `JWT_SECRET_KEY` لازم‌اند؛
- خطر واقعی: کسی تست را **با دیتابیس واقعی آموزشگاه** اجرا کند (نقض قانون پروژه) یا فکر کند پروژه تست ندارد؛
- دو رفتار مهم که در نوبت‌های B1/B2 اضافه شد (اجرا از هر پوشه، CI و گارد DB) جایی مستند نبود.

---

## ۲) رفتار قبل و بعد

| موضوع | قبل | بعد |
|---|---|---|
| `README.md` | یک خط عنوان | معرفی کوتاه دو بخش پروژه + بخش **Running tests** |
| دستور استاندارد تست | فقط در تاریخچهٔ گفتگو/checkpointها | در README با کپی‌پیست کامل (شامل `DATABASE_URL` موقت و `JWT_SECRET_KEY`) |
| قانون «DB واقعی دست نخورد» | ضبط‌شده در قوانین داخلی | در README برجسته + توضیح گارد md5 در CI |
| اجرا از هر پوشه (B1) و CI (B2) | مستند نبود | مستند با مثال‌های اجرایی |

**فایل تغییرکرده:** `README.md` — تنها فایل.

---

## ۳) تست‌ها

| سنجش | نتیجه |
|---|---|
| صحت دستورهای نوشته‌شده در README (تک‌تک اجرا شد) | ✅ `pytest --collect-only` از ریشه: **890 collected** |
| از داخل `Kharazmi_Server/` (bare) | ✅ `test_reports.py` → 10 passed |
| اجرای تست تک با node-id | ✅ 2 passed |
| کل سوئیت پس از تغییر | ✅ **903 passed** (بدون تغییر رفتار — این نوبت فقط مستندات است) |
| md5 دیتابیس واقعی | `f048f8d118b33c4eaa944490594121d7` — **بی‌تغییر** |

---

## ۴) وضعیت build اندروید

**build واقعی انجام نشد و ادعا نمی‌شود** (JDK/SDK در محیط نیست). این نوبت صفر تغییر در سمت اندروید دارد؛ فقط یک یادداشت در README اضافه شد که تست‌های اندروید در CI اجرا نمی‌شوند.

---

## ۵) ریسک‌ها و محدودیت‌های باقی‌مانده

1. **README همچنان کوتاه است** — بخش‌های «نصب روی سرور»، «متغیرهای محیطی کامل» و «ساخت اپ اندروید» مستند نشده‌اند؛ افزودنشان نیازمند تصمیم جداگانه (خارج از B3) است.
2. **`KHARAZMI_UPLOAD_ROOT`** (متغیر جدیدی که در فیکس باگ storage معرفی شد) هنوز در README توضیح داده نشده — چون در نوبت بعدی (فیکس باگ) اضافه می‌شود؛ اگر لازم بود در همان نوبت تکمیل می‌شود.

---

## ۶) Commit و Push

- **Commit SHA:** در گزارش نهایی پس از commit ثبت می‌شود؛ SHA داخل همان commit قابل درج نیست.
- **وضعیت Push:** در گزارش نهایی اعلام می‌شود (همراه با فیکس باگ storage در یک push).
- **Commit message:** `docs: document how to run the test suite`
- **Branch:** `arena/01a0bf8d-kharazmiapp` — PR/merge بدون دستور صریح ساخته نمی‌شود.
- **نوبت بعدی نقشه:** C1 — اعلان واقعی پورتال شاگرد/ولی. (به‌دلیل باگ بحرانی کشف‌شده توسط CI، ابتدا فیکس `storage` در یک commit جدا انجام شد.)
