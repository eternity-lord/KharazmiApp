; FIX(B1) — تثبیت مسیر اجرای تست‌ها (تصمیم B1 در checkpoints/2026-09-20-open-decisions.md)
;
; چرا: پیش از این، هفت فراخوانی `open("Kharazmi_Server/...")` در تست‌ها وابسته به پوشهٔ اجرا بود و
; اجرای سوئیت از داخل `Kharazmi_Server/` پنج failure کاذب (FileNotFoundError) می‌داد. مسیرها در خودِ
; تست‌ها مطلق شدند (نسبت به فایل تست، نه cwd) و این فایل، مسیر پیش‌فرضِ کشف تست‌ها را از ریشه تثبیت
; می‌کند؛ پس `pytest` از هر پوشهٔ داخل ریپو همان مجموعهٔ کامل `Kharazmi_Server/tests` را جمع می‌کند.
; FIX(tests-dir): همهٔ فایل‌های تست اکنون در پوشهٔ `Kharazmi_Server/tests/` هستند (سازمان‌دهی درخواست کاربر).
;
; روش اجرا (مستند پروژه):
;   DATABASE_URL=sqlite:////tmp/test.db JWT_SECRET_KEY=<hex> python3 -m pytest Kharazmi_Server/tests -q
;   — یا از ریشه/هر پوشهٔ دیگر: pytest -q
; نکته: فایل اندروید و مستندات خارج از دامنهٔ تست‌اند (KharazmiAdmin / checkpoints).

[pytest]
testpaths = Kharazmi_Server/tests
python_files = test_*.py
norecursedirs = .* build dist node_modules venv .venv KharazmiAdmin checkpoints
