# O-07 — نمودار درآمد: ۵ میلهٔ ساختگی (کل مبلغ تقسیم بر ۵/۴/۳/۲) به‌جای دادهٔ واقعی

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ · **Branch:** `arena/01a0bf8d-kharazmiapp` · **موج:** ۲ (مورد ۷)
- **Base HEAD:** `cfc326c` (O-08) · **دسته:** مالی/گزارش · **شدت:** بالا (صفحهٔ «نمودارها»ی اپ)

## ریشه
`routers/reports.py:127-133` (`get_chart_data`) نمودار درآمد را **می‌ساخت**:
```python
total_income = sum(t.amount for t in incomes)          # بدون فیلتر نوع/کیف‌پول
income_data = [{"day": "شنبه", "amount": total_income // 5},
               {"day": "یکشنبه", "amount": total_income // 4}, ... ,
               {"day": "چهارشنبه", "amount": total_income}]
```
⇒ «درآمد هر روز هفته» هیچ‌ربطی به تراکنش‌ها نداشت و جمعش ≈ ۲٫۲۸ برابرِ عددی بود که خودش «درآمد» می‌نامید.

## اعداد قبل/بعد روی کپی `/tmp/wave2_o07.db` (کپی `gaj_db.db` با md5 یکسان)
| سناریو | قبل (۵ میلهٔ ساختگی) | بعد (قلم‌های زمانی واقعی) |
|---|---|---|
| کپی دست‌نخوردهٔ دیتابیس توسعه (۱ تراکنش legacy بی‌نوع/بی‌کیف، تاریخ `1404/09/06`) | `[400000, 500000, 666666, 1000000, 2000000]` → جمع **4,566,666** (≈۲٫۲۸×) | ۳۰ قلم روزانه → جمع **0** |
| کپی + فعالیت امروز (۵۰۰ک شمسی، ۷۰۰ک میلادی، ۹۰۰ک معلم، −۴۰۰ک شارژ، ۱۲۰ک بی‌تاریخ) | `[764000, 955000, 1273333, 1910000, 3820000]` → جمع **8,722,333** (≈۲٫۲۸×) | ۳۰ قلم روزانه + قلم «بی‌تاریخ» → جمع **1,200,000** وصولی + ۱۲۰٬۰۰۰ دیده‌شدنی در قلم «بی‌تاریخ» |

## فیکس
| فایل | تغییر |
|---|---|
| `financial_calculations.py` | helper مشترک `collected_revenue_rows(db, start, end, branch_id, course_id, include_undated)`؛ `calculate_institute_collected_revenue` هم رویش بازنویسی شد (منبع یگانهٔ O-08/O-07) |
| `reports.py` | حذف کامل بلوک ساختگی و `total_income`؛ `_income_chart_buckets(rows, start, end)`: ≤۳۱ روز ⇒ قلم روزانه با برچسب تاریخ شمسی، بیشتر ⇒ قلم ماهانهٔ شمسی (`1405/06`)، ردیف بی‌تاریخ ⇒ قلم آخر با برچسب **«بی‌تاریخ»** |
| `KharazmiAdmin/.../strings.xml` | `chart_ds_income`: «درآمد کل بازه (تومان)» → **«وصولی نقدی بازه (تومان)»** |

تصمیم‌های عمدی:
- **قرارداد پاسخ دست‌نخورده:** `income_chart = [{day: str, amount: int}]` — همان `IncomeData(day:String, amount:Float)` در `ChartActivity.kt:43`؛ فقط طول لیست و برچسب‌ها واقعی شدند.
- **بازهٔ نامشخص ⇒ پیش‌فرض «۳۰ روز اخیر»** (همان فیلتر «ماه اخیر» اپ، `ChartActivity:126`) تا قلم‌بندی قطعی باشد و بازهٔ بی‌پایان اسکن نشود.
- **هرگز لیست خالی:** `setupBarChart` (ChartActivity.kt:261) دادهٔ خالی را به نمودار بی‌میله تبدیل می‌کند ⇒ حداقل قلم‌های بازه با مقدار صفر برمی‌گردد.
- **تصمیم E1 حفظ شد:** ردیف بی‌تاریخ حذف نمی‌شود و تاریخ جعلی هم نمی‌گیرد؛ در قلم «بی‌تاریخ» دیده می‌شود. جمع‌های مالی بازه‌دار عمداً فقط تاریخ‌دارها را می‌شمارند (پول بی‌تاریخ در شمارندهٔ اختصاصی O-10 گزارش می‌شود).
- `class_id` → `Transaction.course_id` (هم در helper و هم در نمودار) دست‌نخورده.

## تست
- **قرمز اول:** تست جدید `tests/test_e2e_income_chart_buckets.py` (۷ تست) + بازنویسی `test_12` سناریو ۶؛ روی کد قبلی: `len(income_chart) == 5` میلهٔ ساختگی ⇒ FAILED.
- گاردهای قدیمی که باگ را قفل کرده بودند آگاهانه به‌روزرسانی شدند:
  `tests/test_reports_undated_rows.py` (۹ تست کلاس نمودار: کارخانهٔ داده به `deposit/institute`، جمع «قلم‌های تاریخ‌دار»، قلم «بی‌تاریخ»، حذف assertionهای ۵ میله/«چهارشنبه»)،
  `tests/test_e2e_reports_exports_analytics.py::test_12`، و `test_priority2_financial.py` بدون تغییر سبز ماند.
- سناریو ۶: **18 passed** · فایل جدید: **7 passed** · گارد A3: **22 passed** · کل سوئیت: **1051 passed** · md5 دیتابیس واقعی بی‌تغییر.

## Commit / Push
- پیام: `fix: build the income chart from real cash-collection time buckets (O-07)`
