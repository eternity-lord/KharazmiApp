# Checkpoint — سؤال read-only: is_billed غایب‌ها در تسویه H8 (بدون تغییر کد)

Date: 2026-09-12. فقط خواندن؛ هیچ فیکسی اعمال نشد.

## یافته
- ست‌کردن: فقط Present/Late — `teachers.py:787-793` (آپدیت مشروط H8-P4 با فیلتر
  `is_billed==False + status.in_([Present, Late])`). ردیف Absent برای همیشه False می‌ماند.
- پول سطح-جلسه است نه سطح-ردیف: `total = Σ(final_teacher_cost + absent_penalty_teacher)`
  روی SessionLog (`teachers.py:~780`) — یک‌بار برای هر جلسه.
- گیت P1 (`teachers.py:752-766`): «تسویه‌شده» = هیچ ردیف Present/Late تسویه‌نشده ندارد؛
  هر درخواست شامل جلسه‌ی تسویه‌شده کلاً 400 می‌گیرد، قبل از هر محاسبه.
- نتیجه: ردیف غایبِ False نه در گیت دیده می‌شود (فیلتر status) نه در پول (جمع سطح-جلسه)،
  پس جریمه در تسویه‌ی بعدی دوباره شمرده نمی‌شود — در هر ۶ گزارش طلب هم (pending،
  analytics:323، finance:1950، classes:882، today_summary:302/:538) همین الگوی دوتایی
  «گیت Present/Late + مبلغ سطح-جلسه» تکرار شده و جلسه‌ی تسویه‌شده از همه محو می‌شود.
- تریدآف مستند (کامنت :753-755): جلسه‌ی تماماً-غایب هیچ‌وقت از گیت P1 رد نمی‌شود، یعنی
  جریمه‌اش از این اندپوینت قابل تسویه نیست — به‌عمد، چون اگر رد می‌شد هیچ ردیفی برای
  فلیپ نداشت و جریمه‌اش در هر تسویه‌ی بعدی دوباره شمرده می‌شد (double-pay نامتناهی).
