# گزارش نهایی — موج ۳ (موارد ۱۰ تا ۱۴) + فاز پایانی (بک‌آپ، سناریوها، ممیزی داده)

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ · **Branch:** `arena/01a0bf8d-kharazmiapp` · **PR #1:** باز (دست‌نخورده)
- **Base شروع این نوبت:** `fa8db9b` (O-10) · **پایان (پیش از این سند):** `6caf387`
- **دامنهٔ این سند:** ۵ مورد آخر موج ۳ + کل فاز پایانی (A بک‌آپ، B سناریوها، C ممیزی SELECT، D این گزارش)

---

## ۱) Task — چه چیزی خواسته شده بود
برنامهٔ مصوب کاربر: رفع باگ‌های باز **به ترتیب موج‌ها** و سپس **فاز پایانی**:
(A) بک‌آپ `sqlite3 .backup` + هش + `integrity_check` + **تست بازیابی**؛
(B) اجرای سناریوهای ۱ تا ۶ روی **کپی `/tmp`** با هش قبل/بعد و پاک‌سازی؛
(C) **ممیزی SELECT**: کاربران/سایه‌ها، ردیف‌های بی‌تاریخ، پرداختی > شهریه، ثبت‌نام حذف‌شده، مقطع بی‌تبعه؛
(D) همین گزارش. موارد ممنوع: O-02 / O-12 / O-14 / O-19.

## ۲) Root cause — ریشهٔ هر مورد

| مورد | ریشه (خوانده‌شده از کد) |
|---|---|
| **O-11** | در `routers/admin.py` گارد `delete_teacher` با `db.query(Course).filter(Course.teacher_id == teacher_id).count()` **بدون `is_deleted == False`** شمرده می‌شد ⇒ کلاس آرشیوشده «فعال» حساب می‌شد: هم حذف معلمِ فقط‌آرشیو را ۴۰۰ می‌کرد، هم عدد پیام «N کلاس فعال دارد» را غلط می‌گفت. |
| **O-16** | `scripts/create_admin.py` نام کاربری و رمز را **هارد‌کد** می‌کرد (`09120000000` / `123`) و رمز را **plaintext** ذخیره می‌کرد؛ گارد تکراری روی `username == "admin"` بود نه روی نام کاربری واقعی؛ `sub_role` ست نمی‌شد و موبایل نرمال نمی‌شد. |
| **O-15** | لایهٔ Push دو پیش‌شرط داشت (`FCM_SERVER_KEY` محیطی + ثبت `device_token`) ولی **هیچ endpoint وضعیتی نبود** ⇒ مدیر نمی‌توانست بفهمد Push چرا نمی‌رسد. |
| **O-17** | ردیف `institute_settings` در دیتابیس **۰ ردیف** است و مقادیر از **defaultهای جعلی مدل** می‌آید که مستقیماً روی چاپ رسمی/پورتال ولی می‌رود؛ هیچ فهرستی از «چه چیزی مالک باید پر کند» وجود نداشت. |
| **O-13** | سرور `warnings` را برمی‌گرداند (`admin.py:1924-1964`)، ولی `ClassRestoreResponse` در اندروید **فیلد warnings نداشت** و `MainActivity.restoreArchivedClass` فقط `message` را Toast می‌کرد ⇒ هشدار «کلاس بدون معلم فعال برمی‌گردد» هرگز به مدیر نمی‌رسید. |

## ۳) Before / After

| مورد | قبل (سنجیده‌شده) | بعد (سنجیده‌شده) |
|---|---|---|
| O-11 | معلم با ۱ کلاس آرشیو ⇒ `active_classes=1` و **۴۰۰** (حذف ناممکن)؛ معلم با ۱ فعال + ۱ آرشیو ⇒ پیام «**2** کلاس فعال» | فقط‌آرشیو ⇒ `active_classes=0` و **۲۰۰** (`is_deleted=True`)؛ دارای‌فعال ⇒ پیام «**1** کلاس فعال دارد» |
| O-16 | اسکریپت قدیمی روی `/tmp/o16_old2.db`: ستون `password` = `'123'` (plaintext)، `sub_role=None`؛ اجرای دوباره ⇒ `IntegrityError: UNIQUE constraint failed: users.username` | اسکریپت جدید: رمز هشِ PBKDF2، `role=admin` + `sub_role=admin` + `branch_id=None`، ورود واقعی `200` با `/auth/login` + `/auth/me`؛ اجرای دوباره ⇒ کد خروج ۳ و **هیچ تغییری** |
| O-15 | `GET /dashboard/push_status` ⇒ **404** | `{"fcm_configured": false, "device_token_count": 2}` (ادمین‌فقط؛ کلید هرگز در پاسخ نیست) |
| O-17 | «جدول خالی است؛ نمی‌دانیم چه چیزی مالک باید پر کند» | فهرست مستند ۱۰ فیلد دارای اثر واقعی + ۶ فیلد بی‌اثر (بدون تغییر کد) |
| O-13 | مدل بدون فیلد؛ UI فقط Toast موفقیت | مدل `warnings: List<String>? = null` + دیالوگ «کلاس بازیابی شد — نکته‌های مهم» فقط وقتی هشدار وجود دارد |

## ۴) Files — فایل‌های تغییر‌یافته (این نوبت)

| مورد | فایل‌ها |
|---|---|
| O-11 | `Kharazmi_Server/routers/admin.py` · `Kharazmi_Server/tests/test_admin.py` |
| O-16 | `Kharazmi_Server/scripts/create_admin.py` (بازنویسی) · `Kharazmi_Server/tests/test_create_admin_script.py` (جدید، ۶ تست) |
| O-15 | `Kharazmi_Server/routers/dashboard.py` · `Kharazmi_Server/tests/test_e2e_notifications_push.py` |
| O-17 | فقط سند (صفر کد) |
| O-13 | `KharazmiAdmin/.../AppModels.kt` · `.../MainActivity.kt` · `res/values/strings.xml` · `Kharazmi_Server/tests/test_class_restore_metadata.py` |

## ۵) Checkpoint — اسناد این نوبت
`checkpoints/2026-09-21-o11-archived-classes-teacher-delete.md` ·
`checkpoints/2026-09-21-o16-create-admin-script.md` ·
`checkpoints/2026-09-21-o15-push-status-endpoint.md` ·
`checkpoints/2026-09-21-o17-institute-settings-owner-checklist.md` ·
`checkpoints/2026-09-21-o13-restore-warnings-dialog.md` · **همین سند**

## ۶) Tests

| نقطه | نتیجه |
|---|---|
| O-11 قرمز اول | `tests/test_admin.py` ⇒ **2 failed, 7 passed** (تست‌های جدید O-11) |
| پس از فیکس O-11 | `tests/test_admin.py` ⇒ **9 passed** |
| O-16 | `tests/test_create_admin_script.py` ⇒ **6 passed** (شامل ورود واقعی ادمین ساخته‌شده) |
| O-15 قرمز اول | `test_11_push_status…` ⇒ **404** |
| پس از فیکس O-15 | سناریو ۵ ⇒ **11 passed** |
| O-13 قرمز اول | `test_13_android_client_surfaces_restore_warnings` ⇒ `AssertionError: Regex didn't match '@SerializedName\("warnings"\)\s*val\s+warnings\s*:\s*List<String>\?\s*=\s*null'` |
| پس از فیکس O-13 | `tests/test_class_restore_metadata.py` ⇒ **15 passed** |
| **کل سوئیت** | `1053` (O-10) ⇒ **1061** (O-11+O-16) ⇒ **1062** (O-15) ⇒ **1063 passed** (O-13) |
| `import main` واقعی | سالم روی کپی `/tmp` (`29 routes`) |
| md5 `Kharazmi_Server/gaj_db.db` | `f048f8d118b33c4eaa944490594121d7` — در کل نوبت **بی‌تغییر** |

## ۷) Android build
⚠️ **کامپایل نشد** (JDK/Gradle در سندباکس نیست). O-13 تنها مورد Kotlin این نوبت است: تغییرات
«حداقلی و از الگوهای موجود فایل» (همان `AlertDialog.Builder` و همان `Toast` قبلی)، `strings.xml` با
`xml.dom.minidom.parse` معتبرسازی شد و قرارداد با گارد متنی تست قفل شده است.

## ۸) SHA — زنجیرهٔ کامیت‌ها
```
fa8db9b (O-10)  → 895fa2c (O-11) → 095c1a0 (O-16) → 25501b1 (O-15)
                → 081f29d (O-17) → 6caf387 (O-13) → <این سند>
```
پیام‌ها به‌ترتیب: `fix: allow deleting a teacher whose classes are all archived (O-11)` ·
`fix: make the admin-creator script produce a real, loginable, idempotent admin (O-16)` ·
`feat: expose admin-only push status reporting (O-15)` ·
`docs: list the institute-settings fields the owner must fill (O-17)` ·
`feat: show class-restore warnings to the admin (O-13)`

## ۹) URLs
- Branch: https://github.com/eternity-lord/KharazmiApp/tree/arena%2F01a0bf8d-kharazmiapp
- Commit O-13: https://github.com/eternity-lord/KharazmiApp/commit/6caf387
- CI O-13: https://github.com/eternity-lord/KharazmiApp/actions/runs/35594529679
- PR #1 (باز، دست‌نخورده): https://github.com/eternity-lord/KharazmiApp/pull/1

## ۱۰) Push
همهٔ ۵ کامیت به `arena/01a0bf8d-kharazmiapp` push شدند (`fa8db9b..095c1a0` سپس `095c1a0..081f29d`
سپس `081f29d..6caf387`). CI: `35593577118` (fa8db9b) ✓ · `35593985010` (095c1a0، شامل 895fa2c) ✓ ·
`35594265566` (081f29d، شامل 25501b1) ✓ · `35594529679` (6caf387) ✓ — **همه سبز**.

## ۱۱) Working tree
پیش از این سند: **پاک** (هیچ تغییر uncommitted یا فایل خارج از کامیت). `main` دست‌نخورده (`4e44c87`)،
PR #1 و branchهای دیگر دست‌نخورده. فایل بک‌آپ در `/tmp` نگه داشته شد و **کامیت نشد**.

## ۱۲) Risks — ریسک‌های باقی‌مانده
1. **Push هنوز end-to-end کار نمی‌کند** (O-02 ممنوع + کلید FCM تنظیم نشده)؛ O-15 فقط آن را **قابل‌مشاهده** کرد.
2. **`institute_settings` خالی** ⇒ شماره کارت/آدرس/تلفن جعلی روی چاپ رسمی. اقدام مالک لازم است (O-17).
3. **کمبود داده در دیتابیس توسعه**: ۲ دانش‌آموز/۱ معلم/۱ تراکنش/users=0 ⇒ تست روی دیتابیس **توسعه، نه واقعی**.
4. `max_score` اعشاری (O-14) و انتشار آزمون (O-12) و بازیابی مالی کامل (O-19) باز و **ممنوع** ماندند.
5. هش رمز قدیمی در `ActivityLog.details` (`admin.py~2118`) — طبق دستور **فقط گزارش**، فیکس نشد.

---

# گزارش فاز پایانی

## A) بک‌آپ و تست بازیابی — ✅
`sqlite3` CLI در سندباکس نیست، پس از **API رسمی SQLite در پایتون** (`Connection.backup()`) استفاده شد
که همان `.backup` است (اتصال منبع **فقط‌خواندنی**):
```
ابزار        : python sqlite3 Connection.backup()  (معادل sqlite3 gaj_db.db ".backup …")
بک‌آپ        : /tmp/backup_gaj_db_2026-09-21.db   (520,192 بایت)
md5 بک‌آپ    : 7346e4298f2a4d377b3740bb3d0f9b9b
integrity    : ok  · foreign_key_check: 0 خطا  · 43 جدول
md5 منبع     : f048f8d118b33c4eaa944490594121d7  قبل و بعد از بک‌آپ ⇒ بی‌تغییر
تست بازیابی  : کپی بک‌آپ ⇒ شمارش همهٔ ۴۳ جدول یکسان + integrity ok  ⇒ ✅
```
محتوای غیرخالی دیتابیس توسعه: `branches=1, teachers=1, students=2, courses=1, enrollments=1,
transactions=1, institute_shares=1, pricing_table=4, automation_rules=8, sequence_counters=2`
(این «دیتابیس توسعه» است، نه داده واقعی آموزشگاه).

## B) سناریوهای ۱ تا ۶ روی کپی `/tmp` — ✅
```
cp gaj_db.db /tmp/final_scen.db      ⇒ md5 = f048f8d118b33c4eaa944490594121d7  (هم‌هش با مرجع)
DATABASE_URL=sqlite:////tmp/final_scen.db  pytest ۷ فایل e2e (۶ سناریو + buckets)
نتیجه: 88 passed in 10.83s
هش کپی پس از اجرا: 9c1f58526f59fb3ce55ee143a86a4deb   (طبق انتظار: تست‌ها فایل را لمس می‌کنند)
   ⇒ تنها تفاوت ساختاری: جدول class_restore_logs توسط مایگریشن خودِ startup ساخته شد (DDL idempotent)
   ⇒ صفر تغییر در شمارش همهٔ جدول‌های داده‌ای
md5 دیتابیس واقعی پس از اجرا: f048f8d118b33c4eaa944490594121d7 ⇒ بی‌تغییر ✅
پاک‌سازی: /tmp/final_scen.db + همهٔ DBهای موقت این نوبت حذف شدند
```

## C) ممیزی SELECT روی کپی فقط‌خواندنی — ✅ (صفر نوشتن)
روش: `/tmp/audit_c.db` (کپی `gaj_db.db`) + اتصال sqlite فقط‌خواندنی؛ برای «بی‌تاریخ» از همان تابع
تولیدی `financial_calculations._parse_loose` استفاده شد. خروجی کامل: `/tmp/audit_c_result.txt`.

| # | بررسی | نتیجه |
|---|---|---|
| ۱ | کاربران/سایه‌ها | `users = 0` (هیچ ادمین/منشی وجود ندارد — ریشهٔ O-16 همین بود). `sub_role` خالی: ۰ · کاربر teacher با sub_role ناهمخوان: هیچ · سایهٔ بی‌پرونده: هیچ · سشن با `user_id` بی‌مرجع: هیچ · سشن معلم بدون `teacher_id`: ۰ |
| ۱ب | «معلم فعال بدون سایهٔ User» | **۱ مورد:** معلم id=1 (`09120000000`) — طبق طراحی H20-B2 سایه در **اولین ورود** ساخته می‌شود؛ یافته، نه خرابی |
| ۲ | ردیف‌های بی‌تاریخ | تراکنش‌ها=۱، **بی‌تاریخ=۰**، تاریخ نامعتبر=۰، مبلغ منفی=۰ ⇒ در دیتابیس توسعه هیچ پول بی‌تاریخی وجود ندارد (O-10/B فقط ساختار را افزود) |
| ۳ | پرداختی > شهریه | **۱ مورد:** ثبت‌نام id=1 — شهریهٔ نهایی ۰ ولی `total_paid=2,000,000` ⇒ اضافه‌پرداخت ۲٬۰۰۰٬۰۰۰ (همان بی‌قاعدگی کیف legacy که در O-09 دیده شد؛ علتِ نمایش `due_to_*= −2,000,000`). کیف منفی دانش‌آموز/معلم: هیچ |
| ۴ | ثبت‌نام حذف‌شده / ناسازگاری آرشیو | ثبت‌نام حذف‌شده: هیچ · کلاس آرشیوشده با ثبت‌نام فعال: هیچ · **دانش‌آموز فعال بدون ثبت‌نام فعال: ۱ مورد (id=2)** |
| ۵ | مقطع بی‌تبعه | مقاطع موجود: فقط «کنکور» ×۱ ⇒ `category=None` (بی‌تبعهٔ قیمت‌گذاری). از ۱۵ مقطع پیشنهادی اپ، **«کنکور»، «فارغ‌التحصیل»، «سایر»** هیچ category ندارند · تعرفه‌های بی‌کلاس: `elementary/middle_school/high_school/institute` (هیچ کلاسی به آن‌ها وصل نیست) |

## D) این سند
`checkpoints/2026-09-21-final-phase-report.md`

---

# گزارش ساده (برای تصمیم‌گیری مالک)

۱. **حذف معلم** درست شد: حالا فقط کلاس‌هایی که «فعال» هستند جلوی حذف را می‌گیرند؛ کلاس‌های
   آرشیوشده مانع نمی‌شوند و عدد داخل پیام خطا هم همان تعداد واقعی کلاس فعال است.
۲. **ساخت حساب مدیر** از صفر بازنویسی شد: رمز را خودتان (پنهان) وارد می‌کنید، حداقل ۸ کاراکتر،
   رمز **هش** ذخیره می‌شود و مدیر واقعاً می‌تواند وارد شود. اجرای دوباره هم چیزی را خراب نمی‌کند.
۳. **وضعیت Push** حالا یک اندپوینت ادمین دارد: می‌گوید کلید FCM روی سرور تنظیم شده یا نه و چند
   گوشی ثبت شده است. (Push به‌خودی‌خود روشن نشد؛ اپ اندروید از قبل Firebase ندارد.)
۴. **چک‌لیست تنظیمات آموزشگاه** آماده شد: مدیر باید یک بار تنظیمات را ذخیره کند، بعد ۱۰ فیلد را پر
   کند — مهم‌ترینش **شماره کارت**، چون الان شمارهٔ نمونه روی فاکتور چاپ می‌شود.
۵. **هشدارهای بازیابی کلاس** حالا در یک پنجرهٔ ساده به مدیر نشان داده می‌شود (قبلاً بی‌صدا بود).
۶. **بک‌آپ** گرفته شد، سالم است، و **بازیابی آن تست شد**. سناریوهای ۱ تا ۶ روی یک نسخهٔ کپی اجرا
   شدند (۸۸ تست سبز) و دیتابیس اصلی حتی یک بایت هم تغییر نکرد.
۷. **ممیزی داده** نشان داد دیتابیس موجود یک «دیتابیس توسعه» است: ۲ شاگرد، ۱ معلم، ۱ تراکنش، **هیچ
   کاربری برای ورود**. دو نکتهٔ داده‌ای هم پیدا شد که با پول/مقطع مرتبط‌اند (بخش باگ‌ها).

---

# باگ‌ها (فهرست نهایی)

## رفع‌شده در این نوبت (۵ مورد + ۹ مورد موج‌های قبل)
O-11 · O-16 · O-15 · O-17 · O-13 — و قبل‌تر: O-05, O-06, O-04, O-03, O-01, O-21, O-20, O-08, O-07, O-09, O-10.

## باز و دستوراً ممنوع (اقدام نشد)
| مورد | وضعیت |
|---|---|
| **O-02** | اپ اندروید نه Firebase دارد و نه `device_token` ثبت می‌کند ⇒ Push عملاً خاموش است (نیاز به حساب Firebase + build واقعی) |
| **O-12** | آزمون منتشرنشده (`Exam.status` عملاً مرده) |
| **O-14** | `max_score` اعشاری گرد می‌شود |
| **O-19** | بازیابی کامل مالی کلاس ممکن نیست (فاز ۲ طراحی) |

## باز عملیاتی (نیازمند اقدام مالک — بدون کد)
1. **کلید FCM تنظیم نشده** (`FCM_SERVER_KEY`) ⇒ حتی با ثبت توکن هم ارسالی انجام نمی‌شود.
2. **ردیف `institute_settings` خالی** است ⇒ شماره کارت/آدرس/تلفن/ایمیل **جعلی** روی چاپ رسمی و پورتال ولی.
3. **۶ فیلد بی‌اثر** در تنظیمات: `card_number_1/2`، `card_holder_1/2`، `manager_mobile_1/2` — هیچ‌جا نمایش داده نمی‌شوند.
4. **هیچ حساب ادمین/منشی در دیتابیس نیست** (خودِ O-16 کاربر ساخت را حل کرد، اما اجرای اسکریپت روی DB واقعی با مالک است).

## باز داده‌ای (یافته‌های ممیزی بخش C — نیاز به تصمیم مالک)
5. **ثبت‌نام id=1 اضافه‌پرداخت ۲٬۰۰۰٬۰۰۰ تومانی** با شهریهٔ نهایی ۰ (بقایای کیف قدیمی) ⇒ نمایش «بدهی −۲٬۰۰۰٬۰۰۰» در فاکتور (فیلد جدید `remaining_tuition` درست نشان می‌دهد).
6. **دانش‌آموز id=2 فعال است ولی هیچ ثبت‌نام فعالی ندارد** (شاگرد بی‌کلاس).
7. **مقطع «کنکور»/«فارغ‌التحصیل»/«سایر»** در اپ قابل انتخاب است ولی در قیمت‌گذاری category ندارد ⇒ اگر چنین کلاسی هم نرخ معلم نداشته باشد، ثبت جلسه با خطای ۴۰۰ راهنما (رفتار O-06) رد می‌شود. راه‌حل: یا نرخ جلسه برای کلاس ثبت شود یا دستهٔ قیمت اضافه شود.
8. هیچ کلاسی به تعرفه‌های `elementary/middle_school/high_school/institute` وصل نیست (تعرفه‌ها فقط برای اطلاعات قیمت هستند).

## فقط‌گزارش (طبق دستور فیکس نشد)
9. `admin.py~2118` هش رمز **قدیمی** داخل `ActivityLog.details`.

# تصمیم‌های من (خارج از فهرست، ساده‌ترین گزینهٔ سازگار با کد فعلی)
1. **O-11:** «فعال» = `is_deleted == False`؛ کلاس‌های آرشیوشده دست‌نخورده می‌مانند و بعداً هنگام بازیابی هشدار D5 را می‌دهند. پیام خطا فقط کلاس‌های فعال را می‌شمارد.
2. **O-16:** کدهای خروج `0=ساخته شد · 1=لغو · 2=ورودی نامعتبر · 3=از قبل وجود دارد`؛ نام کاربری = شکل canonical موبایل؛ `branch_id=None` (مدیر کل)؛ idempotent به‌معنای «هیچ تغییر، حتی بازنشانی رمز»؛ تأیید تعاملی پیش از نوشتن + چاپ دیتابیس هدف.
3. **O-16 (تست):** اگر `raw password` در خروجی اسکریپت assert می‌کردم، به‌خاطر **auto-patch startup** (`main.py:527`) ناموفق می‌شد؛ چون هدف واقعی «ذخیرهٔ رمزِ وزیرنشده» است، assert روی ستون ذخیره‌شده (`not plaintext` + `verify_password == True`) گذاشته شد — بدون تضعیف پوشش.
4. **O-15:** بدون کش (وضعیت باید لحظه‌ای باشد)، فقط‌خواندنی، ادمین‌فقط، نام فیلد `device_token_count`، و هرگز خودِ کلید در پاسخ. تست برای نقش‌ها ۴۰۱/۴۰۳ را می‌پذیرد چون سشن معلمِ دنیای آزمایشی به کاربر بی‌رکورد وصل است (در متن تست مستند شد).
5. **O-17:** «صفر نوشتن» ⇒ فقط خواندن کد/دیتابیس کپی؛ فیلدهای بی‌اثر **حذف/تغییر نشدند** چون خارج از دامنه بود.
6. **O-13:** دیالوگ فقط وقتی هشدار وجود دارد (بی‌هیچ تغییری در مسیر موفق معمولی)؛ فیلد nullable برای سازگاری با سرور قدیمی؛ گارد به‌صورت اسکن سورس کاتلین چون build ممکن نیست.
7. **فاز پایانی:** معادل `.backup` خودِ SQLite از API پایتون استفاده شد (نبود CLI)؛ فایل بک‌آپ **کامیت نشد** (خارج از گیت)؛ سناریوها و ممیزی فقط روی کپی `/tmp`؛ همهٔ DBهای موقت پس از کار پاک شدند.
