# گزارش تجمیعی و نقشهٔ رسیدن به ثبات — سامانهٔ خوارزمی

- **تاریخ:** ۲۰۲۶-۰۹-۲۱ · **Branch:** `arena/01a0bf8d-kharazmiapp`
- **مرجع‌های این سند:** `checkpoints/2026-09-20-open-decisions.md` (نقشهٔ ۲۶ تصمیم) ·
  `checkpoints/2026-09-21-final-report.md` (گزارش فاز اول + بخش ۱۰ این فاز) ·
  `checkpoints/2026-09-21-e2e-debug-scenario-{1..6}-*.md` (گزارش تفصیلی هر سناریو)
- **این سند فقط گزارش و برنامهٔ پیشنهادی است — هیچ کدی در آن تغییر نکرده.**

---

# بخش ۱ — تصویر کلی در یک نگاه

| سنجه | مقدار |
|---|---|
| تسک‌های کدنویسی‌شدهٔ تحویل‌شده | **۲۰ تسک** (۷ تسک اولیه + A1–A4 + B1–B3 + storage + C1 + C2 + D1 + C6 + D5) |
| تصمیم‌های نقشهٔ ۲۶تایی | **همه تعیین‌تکلیف شد**: ۱۳ مورد کدنویسی شد، ۱۳ مورد با تصمیم مستند (بدون کد) |
| تست‌های جدید این برنامه | **+۱۶۶ تست** (فاز اول) و **+۷۲ تست e2e** (فاز دیباگ اخیر) |
| کل سوئیت تست | **۱۰۳۸ passed** |
| CI (GitHub Actions) | ✅ روی همهٔ pushها سبز |
| دیتابیس واقعی `gaj_db.db` | **دست‌نخورده** — md5 `f048f8d118b33c4eaa944490594121d7` در تمام مراحل |
| PR #1 / branch قدیمی / `main` | همه دست‌نخورده (طبق قانون) |
| باگ‌های رفع‌شده | **۱۵ مورد** (جدول بخش ۲) |
| مشکلات باز با راه‌حل آماده | **۱۸ مورد** (بخش ۴ و ۵ — به ترتیب اولویت) |

---

# بخش ۲ — چه چیزهایی رفع شد (فاز اول: ۲۰ تسک)

| # | مشکل قبلی | چطور رفع شد | Commit |
|---|---|---|---|
| F-01 | نام‌های خالی/NULL داده‌های معلم در پاسخ‌ها «None None» می‌داد | null-safe کردن تابع‌های نمایش نام در سرور | `8f48590` |
| F-02 | پنل ادمین کلاس‌های آرشیوشده را ناقص نشان می‌داد | مسیر و پاسخ‌های آرشیو یکدست شد | `3e9da85` |
| F-03 | درخواست‌های تأیید معلم در اپ نمایش داده نمی‌شد | اتصال فهرست درخواست‌ها به پنل | `470fe96` |
| F-04 | اعلان‌های ادمین ساخته می‌شد ولی به کاربر نمی‌رسید | مسیر خواندن اعلان ادمین | `6dace62` |
| F-05 | جلسات بی‌تاریخ از تسویهٔ معلم می‌افتاد ⇒ طلب معلم کم‌تر از واقع | محاسبه با date-parser مشترک | `b676ff6` |
| F-06 | شکست‌های موجود تست، مستند نشده بود | مستندسازی | `b167902` |
| F-07 | طراحی مسیر «بازیابی کلاس» نبود | ممیزی امنیت بازیابی (سند) | `124392d` |
| F-08 | نقش‌های فرعی به داده/عملیات حساس دسترسی داشتند | سیاست کمترین دسترسی + گارد نقش کانونیکال | `f271bc6` |
| F-09 | «رد کلاس» جلسات را آرشیو نمی‌کرد ⇒ طلب معلم از کلاس حذف‌شده باز می‌ماند | یکدست‌سازی با سایر مسیرهای حذف | `c813570` |
| F-10 | گزارش‌های مالی ردیف بی‌تاریخ را حذف می‌کردند | نگه‌داشتن ردیف‌های بی‌تاریخ | `04d2e1e` |
| F-11 | نام فرد در کل سرور (۳۵ سناریو) NULL-safe نبود | null-safe سراسری | `acb8d71` |
| F-12 | ۵ تست فقط از پوشهٔ خاصی سبز بودند (CI گمراه‌کننده) | مستقل‌سازی از پوشهٔ اجرا | `e72da53` |
| F-13 | هیچ CI نبود | GitHub Actions با DB موقت | `3381999` |
| F-14 | روش اجرای تست‌ها مستند نبود | README | `7f687bb` |
| F-15 | **آپلود فایل در مسیر مطلق `/home/user/uploads`** (تکالیف/کارنامه/پیام) و عکس پروفایل **نسبت به پوشهٔ اجرا** ⇒ روی CI/سرور واقعی فایل‌ها گم/۴۰۴ می‌شد | ماژول `storage.py` + ریشهٔ واحد زیر پروژه + مسیرهای قدیمی فقط برای خواندن/حذف (شامل پروفایل و اندپوینت محافظت‌شدهٔ `/uploads/{file}`) | `c4a01c8` |
| F-16 | **اعلان‌های ساختگی در پورتال ولی/دانش‌آموز** (پیام‌های جعلی، اعلان واقعی نمی‌آمد) | خواندن از جدول واقعی `notifications` | `23d3343` |
| F-17 | **ارسال Push در سرور هرگز اتفاق نمی‌افتاد** (حلقهٔ خالی) | لایهٔ Push واقعی با گارد محیطی و پاک‌سازی توکن مرده | `a444449` |
| F-18 | حذف اشتباهی کلاس ⇒ هیچ راه بازگشتی نبود | «بازیابی فقط متادیتا» + دکمهٔ اپ (پول/ثبت‌نام برنمی‌گردد) | `daa01b4` |
| F-19 | **تکالیف/آزمون/برنامهٔ هفتگی جعلی در پورتال‌ها** | داده‌های واقعی از دیتابیس | `d6531ec` |
| F-20 | بازیابی کلاس با معلم آرشیوشده بی‌هشدار بود | فیلد `warnings` در پاسخ | `109c34d` |
| F-21 | تست‌ها پراکنده در پوشهٔ سرور بودند | انتقال به `Kharazmi_Server/tests/` | `9396623` |

---

# بخش ۳ — فاز دیباگ e2e (این نوبت): ۶ سناریو، صفر تغییر کد

| سناریو | مسیر سنجیده‌شده | تست | Commit |
|---|---|---|---|
| ۱ | ورود → نشست → توکن دستگاه → خروج + OTP دانش‌آموز | ۱۱ | `0266611` |
| ۲ | کلاس → ثبت‌نام → حضور و غیاب → اثر مالی | ۱۰ | `84c5dd8` |
| ۳ | تکلیف/آزمون: ثبت معلم → شاگرد → تصحیح → پورتال ولی | ۱۲ | `12777b2` |
| ۴ | مالی: پرداخت، قسط، فاکتور، رسید | ۱۲ | `69d366c` |
| ۵ | اعلان‌ها و Push | ۱۰ | `57df71c` |
| ۶ | گزارش‌ها، خروجی‌ها، تحلیل‌ها | ۱۷ | `568acaa` |

**رفتارهای سالم تأییدشده (به‌عنوان تضمین باقی می‌مانند):** ضد پرداخت تکراری (idempotency) ·
جدا‌سازی اعلان هر کاربر · گارد IDOR ولی · گارد مالکیت معلم · سیاست «سهم‌ها فقط برای ادمین» ·
محافظت CSV injection + BOM فارسی · اکسل راست‌به‌چپ · خروجی معوقات با دسته‌بندی بحرانی ·
صورت‌حساب شاگرد با اعداد واقعی · ریت‌لیمیت ورود (۵ تلاش/۵ دقیقه) · OTP هش‌شده و یک‌بارمصرف ·
قفل پورتال برای شاگرد بی‌دسترسی.

---

# بخش ۴ — فهرست کامل مشکلات باز (با شاهد تست)

| ID | مشکل | دسته | شدت | شاهد (تست) |
|---|---|---|---|---|
| O-01 | ورود دوم معلم ⇒ پاسخ `{role:"admin", sub_role:"teacher"}` و هدایت اپ به پنل ادمین | سرور + اندروید | **بالا** | سناریو ۱ · `test_4` |
| O-02 | اپ اندروید هیچ کلاینت FCM/`device_token` ندارد ⇒ Push مرده | اندروید | **بالا** | سناریو ۱ (`test_9`) · ۵ |
| O-03 | خروج اپ فقط `finishAffinity()` ⇒ نشست/توکن سرور زنده می‌ماند | اندروید | متوسط | سناریو ۱ · `test_9` |
| O-04 | دانش‌آموزِ حذف‌شده از کلاس: تکلیف می‌بیند، تحویل می‌دهد، آزمون می‌دهد، اعلان می‌گیرد (۷ مسیر) | منطق کد | **بالا** | سناریو ۳ · `test_2/8/11` |
| O-05 | «چاپ حواله» همیشه ۵۰۰ (`time` ایمپورت نشده) | سرور | **بالا** | سناریو ۴ · `test_12` |
| O-06 | نبود تعرفه ⇒ ثبت حضور و غیاب با **۵۰۰** رد می‌شود (به‌جای خطای قابل‌فهم) | سرور/UX | متوسط | سناریو ۲ · `test_8a/8b` |
| O-07 | نمودار درآمد، دادهٔ ساختگی (`total//5..total`) ⇒ جمع ۲.۳ برابر واقعیت | منطق کد | **بالا** | سناریو ۶ · `test_12` |
| O-08 | «درآمد امروز» و نمودار = جمع **علامت‌دار همهٔ** تراکنش‌ها (شارژ جلسه از درآمد کم می‌شود؛ پرداخت کیف معلم درآمد مؤسسه حساب می‌شود) | مالی | **بالا** | سناریو ۶ · `test_12/14` |
| O-09 | «بدهی» فاکتور بعد از پیش‌پرداخت جزئی **منفی** نشان داده می‌شود | مالی/نمایش | متوسط | سناریو ۴ · `test_11` |
| O-10 | پرداخت بی‌تاریخ در «وصول‌شدهٔ ماه» نمی‌آید ولی در صورت‌حساب دیده می‌شود | گزارش | کم | سناریو ۶ · `test_1/4` |
| O-11 | `delete_teacher` با کلاسِ **آرشیوشده** هم حذف را رد می‌کند و پیام «N کلاس فعال» می‌دهد | منطق کد | کم | ممیزی قبلی · `admin.py:310` |
| O-12 | آزمون‌ها بدون «انتشار» برای شاگرد دیده می‌شوند (ستون `status` عملاً مرده است) | محصولی | متوسط | ممیزی قبلی · `exams.py:45/136` |
| O-13 | پاسخ «بازیابی کلاس» فیلد `warnings` دارد ولی اپ آن را نشان نمی‌دهد | اندروید | کم | D5 · `MainActivity` |
| O-14 | `max_score` اعشاری به بالا/پایین گرد می‌شود (کلاینت Int می‌خواهد) | اندروید | کم | C6 |
| O-15 | روی سرور `FCM_SERVER_KEY` تنظیم نشده ⇒ Push خاموش است (طراحی‌شده) | عملیاتی | متوسط | C2 |
| O-16 | **هیچ حساب ادمین/منشی در دیتابیس نیست ⇒ ورود مدیر ناممکن است** | عملیاتی/داده | **بالا** | سنجش DB |
| O-17 | جدول `institute_settings` خالی است (گزارش‌ها به پیش‌فرض می‌افتند) | عملیاتی/داده | کم | سنجش DB |
| O-19 | بازیابی **کامل مالی** کلاس (ثبت‌نام/جلسه/پول) هنوز ممکن نیست | طراحی (فاز ۲) | متوسط | ممیزی `124392d` |

**دو اصلاح پس از بازبینی دقیق (برای اینکه فهرست «باز» تمیز باشد):**

> **۱) بند «اگر `InstituteSettings` نباشد در چاپ `None` می‌آید» بازتولید نشد** — مسیرهای گزارش در نبود
> رکورد، خودشان یک رکورد پیش‌فرض می‌سازند (`reports.py:571-575`). ⇒ بسته است.
> **۲) O-18 («مسیر عکس پروفایل نسبت به پوشهٔ اجرا») هم‌اکنون باز نیست** — در بازبینی مشخص شد همان
> commit `c4a01c8` این مسیر را هم اصلاح کرده است (`main.py:89` → `storage_dir("profiles")` و اندپوینت
> محافظت‌شدهٔ `/uploads/{filename}` با fallback مسیرهای قدیمی). ⇒ به F-15 منتقل شد.

---

# بخش ۵ — راه‌حل هر مشکل (تا رسیدن به ثبات)

هر راه‌حل با این قالب آمده: **تغییر فنی · تست تأیید · اثر مالی · حجم/ریسک · نیاز به تصمیم شما؟**

### O-01 — ورود دوم معلم (بالا)
- **تغییر:** در `routers/auth.py` شاخهٔ ورود معلم (خطوط ۱۲۰-۱۹۶): وقتی تطبیق از روی «کاربر سایه»
  انجام می‌شود، پاسخ باید مثل ورود اول نرمال شود: `role="teacher"`, `sub_role="teacher"`,
  `user_id=teacher.id` (همان چیزی که الان `/auth/me` برمی‌گرداند). اعتبارسنجی: توکن همچنان
  `teacher_id` را در `UserSession` نگه دارد.
  در اندروید `LoginActivity.kt:273-280` هدایت بر اساس `sub_role == "teacher"` انجام شود (نه `role == "admin"`).
- **تست:** `test_4_second_teacher_login_returns_admin_role_bug` **آگاهانه** به تست صحیح تبدیل شود
  (`test_4_second_teacher_login_returns_teacher_scope`) + تست اندرویدِ مسیرِ تصمیم (static guard).
- **اثر مالی:** ندارد. **حجم:** S · **ریسک:** کم (تست قفل‌کننده دارد). **تصمیم شما:** لازم نیست (باگ است).

### O-02 — Push در اندروید (بالا)
- **تغییر (سه گام):** ۱) افزودن `firebase-messaging` به Gradle + فایل `google-services.json` از پنل Firebase آموزشگاه.
  ۲) سرویس دریافت توکن + فراخوانی `POST /auth/device_token` (سرور آماده و تست‌شده است) در زمان ورود.
  ۳) فراخوانی `POST /auth/logout?device_token=...` + حذف توکن محلی در خروج.
- **تست:** سرور از قبل تست دارد؛ سمت اندروید تست خودکار ندارد ⇒ پس از build، تست دستی «اعلان دریافت شد».
- **اثر مالی:** ندارد. **حجم:** M/L · **ریسک:** متوسط (نیازمند build واقعی). **تصمیم شما:** برای Google/Firebase حساب آموزشگاه لازم است.

### O-03 — خروج بدون logout (متوسط)
- **تغییر:** `SettingsActivity.kt:79-88` → قبل از `finishAffinity()`: `POST /auth/logout` (با `device_token` اگر موجود)،
  پاک‌سازی `SecureLoginStore`، سپس بستن. (اندپوینت سرور آماده و تست‌شده.)
- **تست:** گاردِ static (مانند `test_9` سناریو ۱) + تست سرور موجود. **مالی:** ندارد · **حجم:** S.

### O-04 — دسترسی دانش‌آموزِ حذف‌شده (بالا)
- **تغییر:** افزودن فیلتر `Enrollment.is_deleted == False` (و به‌ترتیب امنیت بیشتر `Course.is_deleted == False`)
  در: `homework.py:94,158,203,315` و `exams.py:75,136,187`.
- **تست:** سه تستی که فعلاً باگ را «قفل» کرده‌اند آگاهانه به تست‌های انتظار-درست تبدیل شوند:
  سناریو ۳ `test_2`، `test_8`، `test_11` (انتظار ۴۰۳/حذف از لیست).
- **اثر مالی:** ندارد · **حجم:** S · **ریسک:** کم · **تصمیم شما:** لازم نیست.

### O-05 — چاپ حواله ۵۰۰ (بالا)
- **تغییر:** `routers/finance.py` → افزودن `import time` در بالای فایل (یا جایگزینی
  `int(time.time())` با `int(datetime.datetime.now().timestamp())` — سبکِ خودِ فایل).
- **تست:** سناریو ۴ `test_12` از «انتظار ۵۰۰» به «انتظار ۲۰۰ + `print_job_id` با پیشوند PRINT_» تبدیل شود.
- **مالی:** ندارد · **حجم:** S (یک خط) · **تصمیم شما:** لازم نیست.

### O-06 — کد ۵۰۰ برای تنظیمات ناقص (متوسط)
- **تغییر:** در `financial_calculations.py` (خطوط ۲۷۵/۲۸۹/۳۵۶) کد ۵۰۰ به **۴۰۰** (یا ۴۰۹) تغییر کند و
  پیام راهنما همان بماند. اپ همان پیام را نشان می‌دهد (بهبود UX بدون تغییر منطق).
- **اختیاری (پیشنهاد):** در راه‌اندازی سرور، اگر `institute_shares`/`pricing_table` خالی بودند،
  مقادیر پیش‌فرض منطقی seed شود (الگوی موجود Auto-Patch در `main.py`) تا این حالت اصلاً پیش نیاید.
- **تست:** سناریو ۲ `test_8a/8b` آگاهانه به انتظار ۴۰۰ تغییر کند. · **مالی:** ندارد · **حجم:** S.

### O-07 — نمودار درآمد ساختگی (بالا)
- **تغییر:** `reports.py:127-133` → محاسبهٔ واقعی: برای روزهای بازه (یا ۷ روز اخیر) جمع **پرداخت‌های مثبت**
  با `parse_project_date` گروه‌بندی شود؛ روزهای بی‌داده صفر بمانند. تعریف «درآمد» عیناً از O-08 بیاید
  (تابع مشترک) تا نمودار و KPI یک عدد بدهند.
- **تست:** سناریو ۶ `test_12` بازنویسی شود (این تست فعلاً الگوی ساختگی را قفل کرده است).
- **مالی:** بله (نمایش عدد) — نیازمند تأیید شما. **حجم:** M · **ریسک:** متوسط · **تصمیم:** ✅ لازم.

### O-08 — تعریف واحد «درآمد» (بالا، مالی)
- **تغییر:** یک تابع مشترک در `financial_calculations.py` (کنار `calculate_institute_collected_revenue:60`):
  درآمد = `SUM(amount > 0)` با `type IN ('deposit','enrollment_payment','tuition')` و
  `target_wallet IN ('institute','both')` و `is_deleted=False` و `is_reversed=False` و فیلتر شعبه/تاریخ.
  مصرف‌کننده‌ها: `dashboard.py:_today_revenue_sql` و نمودار `reports.py`. **شارژ جلسه (بدهی) از درآمد کم نمی‌شود.**
- **تست:** تست‌های `test_12/14` سناریو ۶ + **تست بی‌اثری**: «ثبت یک جلسه (شارژ منفی) عدد درآمد را تغییر ندهد».
- **مالی:** بله — تغییر اعداد گزارش/KPI (به سمت درست). **حجم:** M · **تصمیم:** ✅ لازم (طبق قانون شما: «منطق مالی خراب نشود»).

### O-09 — «بدهی» منفی روی فاکتور (متوسط)
- **تغییر (بدون شکستن چیزی):** در `finance.py:679-704` دو فیلد افزوده شود:
  `remaining_tuition = max(0, final_tuition - paid_total)` و `credit_balance = max(0, -remaining_tuition)`;
  فیلدهای فعلی (`due_to_*`) دست‌نخورده بمانند (سازگاری عقب‌رو).
  در `InvoiceActivity.kt:310-322` «بدهی» از `remaining_tuition` نمایش داده شود و مقدار پیش‌فرض پرداخت همان شود.
- **تست:** سناریو ۴ `test_11` گسترش یابد (هر دو دستهٔ فیلد) + تست بی‌اثری اعداد قدیمی.
- **مالی:** بله (نمایش) · **حجم:** M · **تصمیم:** ✅ لازم.

### O-10 — پرداخت بی‌تاریخ در وصول‌شدهٔ ماه (کم)
- **تغییر:** به پاسخ `financial_summary` دو فیلد افزوده شود: `undated_count` و `undated_amount`
  (بدون تغییر جمع‌های فعلی)؛ اپ در صفحهٔ گزارش یک خط هشدار نشان دهد: «N پرداخت بی‌تاریخ خارج از این ماه: مبلغ X».
- **تست:** سناریو ۶ `test_1` گسترش یابد. · **مالی:** بله (افزودنی) · **حجم:** S/M · **تصمیم:** ✅ (پیشنهاد پیش‌فرض: همین روش).

### O-11 — حذف معلم با کلاس آرشیوشده (کم)
- **تغییر:** در `routers/admin.py:310-325` شمارش فقط کلاس‌های **فعال** (`Course.is_deleted == False`) باشد؛
  اگر تعداد صفر شد، حذف معلم مجاز شود و در پاسخ بیاید «کلاس‌های آرشیوشده دست‌نخورده ماندند».
  (بازیابی بعدی چنین کلاسی هشدار D5 را نشان می‌دهد.)
- **تست:** دو تست: مسیر «فقط آرشیوشده ⇒ مجاز» و «کلاس فعال ⇒ رد با پیام درست». · **مالی:** ندارد (باید بی‌اثری روی تسویه اثبات شود) · **حجم:** S · **تصمیم:** ✅ (پیش‌فرض پیشنهادی: اجازه در نبود کلاس فعال).

### O-12 — آزمون منتشرنشده (متوسط، محصولی)
- **تغییر پیشنهادی (سازگار با دادهٔ موجود):** ۱) `create_exam` به‌جای `status="pending"` مقدار
  `status="published"` بگذارد (یعنی رفتار فعلی حفظ شود: همه می‌بینند). ۲) یک اندپوینت
  `POST /exams/{id}/publish` و `POST /exams/{id}/unpublish` (معلم مالک) اضافه شود.
  ۳) `get_student_exams_list` فقط `status == "published"` را نشان دهد.
- **تست:** ساخت آزمون → شاگرد می‌بیند؛ unpublish → شاگرد نمی‌بیند (۴۰۴/حذف از لیست). · **مالی:** ندارد · **حجم:** M · **تصمیم:** ✅ (اگر «آزمون پیش‌نویس» نمی‌خواهید، این مورد را نادیده بگیریم).

### O-13 — نمایش `warnings` بازیابی در اپ (کم)
- **تغییر:** در `MainActivity` (بخش کلاس‌های حذف‌شده) پاسخ restore خوانده و `warnings[]` در یک دیالوگ
  تأیید/اطلاع نشان داده شود (الگوی `AlertDialog` موجود در همان فایل).
- **تست:** static guard روی وجود کلید `warnings` در اپ + تست سرور موجود. · **حجم:** S · **تصمیم:** لازم نیست.

### O-14 — `max_score` اعشاری (کم)
- **دو راه:** (الف) «همین‌طور بماند» (گردکردن نیم‌به‌بالا — کلاینت فعلی Int می‌خواهد) یا
  (ب) تغییر `ParentExamItem.max_score` به `Double` + ارسال دقیق از سرور (نیازمند build).
- **پیشنهاد:** (الف) تا زمان build بعدی؛ در آن build به (ب) سوئیچ شود. · **حجم:** S · **تصمیم:** ✅ اختیاری.

### O-15 / O-16 / O-17 — کارهای عملیاتی روی دیتابیس و سرور (بالا برای O-16)
- **O-16 (فوری):** ساخت حساب ادمین. پیشنهاد: اسکریپت `Kharazmi_Server/create_admin.py`
  (`--mobile`، `--full-name`، رمز از ورودی ترمینال، idempotent، بدون رمز پیش‌فرض) + تست خودکار که
  «با این حساب، ورود ادمین و `/auth/me` کار می‌کند». بدون این، پنل مدیریت اصلاً باز نمی‌شود.
- **O-17:** ثبت رکورد `InstituteSettings` (نام/آدرس/کارت/تلفن) — از پنل تنظیمات یا با همان اسکریپت
  (`--seed-institute-settings`). مسیرهای چاپ فعلاً پیش‌فرض نشان می‌دهند.
- **O-15:** تنظیم `FCM_SERVER_KEY` روی سرور آموزشگاه + (پیشنهاد) اندپوینت تشخیصی
  `GET /dashboard/push_status` (ادمین‌فقط) که `fcm_configured` و تعداد توکن‌های ثبت‌شده را بدهد.
- **حجم:** S · **ریسک:** کم · **تصمیم:** ✅ برای ساخت ادمین (نیاز به شماره/نام واقعی).

### O-19 — بازیابی کامل مالی (فاز ۲، نیازمند طراحی)
- **پیش‌شرط:** «دفتر کلّ حذف» سمت حذف: ثبت شناسه‌ها و `deleted_at` و ثبت سند مالی معکوس.
  سپس بازیابی کامل با «حرکت پول با سند» (نه زنده‌کردن خام) ممکن می‌شود؛ بدون آن، ریسک double-count و
  تضاد ایندکس یکتای جلسه وجود دارد. طراحی کامل در `checkpoints/2026-09-20-open-decisions.md` (D1/D7).
- **حجم:** L · **تصمیم:** ✅ لازم (پیشنهاد: بعد از تثبیت موارد بالا).

---

# بخش ۶ — نقشهٔ رسیدن به ثبات (ترتیب پیشنهادی)

**تعریف «ثبات» (Definition of Done هر مورد):** تغییر کوچک و متمرکز + تست گرفته‌شده (قفل‌کننده) +
کل سوئیت ۱۰۳۸+ سبز + CI سبز + اثبات بی‌اثری روی اعداد مالی (وقتی مالی است) + دیتابیس واقعی دست‌نخورده.

| موج | موارد | چرا این ترتیب |
|---|---|---|
| **موج ۱ — باگ‌های بی‌ابهام (بدون تصمیم)** | O-05 (چاپ حادثهٔ ۵۰۰) · O-06 (کد خطا) · O-04 (دسترسی شاگرد حذف‌شده) · O-03 (خروج اپ) · O-01 (ورود دوم معلم) | خطاهای واضح، تست قفل‌کنندهٔ آماده، صفر اثر مالی |
| **موج ۲ — اعداد و گزارش‌ها (نیازمند تأیید شما)** | O-08 · O-07 · O-09 · O-10 | تعریف واحد درآمد و نمایش بدهی؛ با «تست بی‌اثری» و مقایسهٔ قبل/بعد اعداد |
| **موج ۳ — عملیاتی (باز شدن پنل مدیریت و Push)** | O-16 · O-15 · O-17 | بدون O-16 ورود مدیر ممکن نیست؛ بدون O-15 اعلان گوشی نداریم |
| **موج ۴ — اندروید (نیازمند build)** | O-02 · O-03(سمت اپ) · O-13 · O-14 | یک build واقعی لازم دارد (`./gradlew assembleDebug`) |
| **موج ۵ — محصولی و فاز ۲** | O-12 · O-11 · O-19 | تصمیم‌های محصولی/طراحی بزرگ‌تر |

---

# بخش ۷ — گزارش ساده (برای فهم غیرفنی)

**جمع‌بندی: سامانه در فاز اول ۲۰ مشکل واقعی‌اش برطرف شد (از پیام‌های ساختگی والدین و تکالیف جعلی تا
گم‌شدن فایل‌ها و حساب‌های مالی) و در فاز دوم با ۷۲ آزمون واقعی از دید خودِ اپ محک زده شد. نتیجه:
حساب‌ها و پول سالم است، ولی ۱۸ موضوع باقی مانده که ۶ تای آن‌ها مهم‌اند.**

**۶ موضوع مهم باقی‌مانده (به زبان ساده):**
1. **اعلان گوشی کار نمی‌کند** — سرور آماده است، اپ موبایل بخش اعلان را ندارد.
2. **دو عدد «درآمد» در پنل ادمین غلط است** — یکی ساختگی است و دیگری هزینه‌های ثبت‌شده را از درآمد کم می‌کند.
3. **دکمهٔ «چاپ رسید» همیشه خطا می‌دهد.**
4. **دانش‌آموزی که از کلاس حذف شده، هنوز تکلیف می‌بیند و آزمون می‌دهد.**
5. **ورود دوم معلم او را به پنل مدیر می‌فرستد** و آنجا هیچ کاری نمی‌تواند بکند.
6. **در دیتابیس فعلی هیچ حساب مدیری ساخته نشده** — یعنی همین امروز پنل مدیریت باز نمی‌شود.

**راه‌حل‌ها آماده است:** برای هر مورد در بخش ۵ دقیقاً نوشته شده چه تغییری لازم است، چه تستی باید پاس شود
و چه ریسکی دارد. موج ۱ (۵ مورد اول از جدول بخش ۶) کار کوچکی است و بدون تصمیم شما قابل انجام؛
موج ۲ عددهای مالی را تغییر می‌دهد (به سمت درست) و طبق قانون خودتان فقط با تأیید شما انجام می‌شود.

---

# بخش ۸ — وضعیت GitHub

- **Branch:** `arena/01a0bf8d-kharazmiapp` — آخرین commit: `3dd29001265302d5333a226da2e1526f22d68697`
- **CI:** ✅ سبز روی همهٔ pushها (آخرین: `35584579166`)
- **main:** `4e44c87` دست‌نخورده · **PR #1:** باز و دست‌نخورده
- **دیتابیس واقعی:** دست‌نخورده (md5 `f048f8d118b33c4eaa944490594121d7`)
