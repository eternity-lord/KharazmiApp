# Checkpoint — H16-IDOR fix (POST print/pdf)

Date: 2026-09-12. 2 functions patched, line-asserted + visual read-back.
No compile/run.

## Pattern copied (GET /finance/receipt/{id}, finance.py:1666+)
- Signature carries `authorization: Optional[str] = Header(None)` alongside
  `check_user_login`; after txn-404 and BEFORE any PII lookup:
  `verify_financial_idor(<student_id>, authorization, db)`.
- Verifier (:603): no/bad token → 401; admin/secretary → full pass; student/
  parent → 403 unless own.id == student_id; anything else → 403.

## Fix
- finance.py:433 `print_receipt`: +authorization param; +2-line IDOR guard
  (:445-446) between txn-404 and student lookup. Old :433 / new :435 shift +2.
- finance.py:475→477 `generate_pdf_receipt`: identical two changes (:477 sig,
  :489-490 guard).
- Header + Optional already imported (:2, :4) — no import change.
- Order mirrors GET exactly: txn-404 → IDOR → student-404 → receipt_data.
  Success-path bytes unchanged; check_user_login kept (no new 401 surface:
  legit callers already send the token this same header).
- No app/web callers (stub declarations only) → zero client regression; the
  only behavior delta is the intended 403 for cross-user access.
