# Triage 24 remaining failures (read-only, diagnostic runs only — no fixes)
## (B) LIVE BUGS — 11 tests, 2 roots
- B1 analytics.py:203-204 (H3-B3 regression): `db.query(SessionLog.date).join(SessionLog).join(Course)` — join entity به کوئری ستونی؛ GET /analytics/dashboard همیشه 500. قربانیان (10): automation x2, financial_reads_share, tuition x6+branch. اپ صدایش نمی‌زند (no .kt ref) → portal-only. Medium-low.
- B2 main.py patches-list (H8-gap regression): کامای گمشده بعد از تاپل M13 → `('tuple')('tuple')` = TypeError at list-build → auto_patch_database کاملاً می‌میرد (بنر قرمز در هر import). قربانی: schema_upgrade (1). Fresh-install سالم (create_all + ایندکس در metadata :315)؛ DBهای موجودِ نیازمند پچ از H8-gap به بعد هیچ پچی نگرفتند. One-comma fix. Medium.
## (A) STALE TESTS — 13 tests
- security_audit (1): هر 21 روت flagged با check_admin_or_secretary_access محافظت‌اند (deps-map اسکریپت)؛ تست فقط 2 dep قدیمی را می‌شناسد. False-positive؛ چیز جدید/خطرناکی نیست.
- Dangling-session/linkage (7): advanced_idor, ai x2, my_profile(404), three_critical x3 (Student.user_id لینک نشده؛ teacher فقط چون موبایل==username کار می‌کند).
- Bulk-staleness refresh-class (2): refund (سرور خودش در کامنت می‌گوید stale می‌شود!), delete_enrollment.
- Gregorian-era (1): soft_delete_e2e (seed/params میلادی؛ بعد از فیکس تاریخ به B1 می‌رسد — ترتیب مهم است).
- M12-era (1): legacy wallet param (سرور عمداً 400 می‌دهد؛ اپ legacy ندارد).
- Soft-delete-era text (1): admin delete message.
