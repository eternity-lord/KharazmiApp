# Issue #13 impl — canonical Jalali session date (2026-09-12, no compile/run)
- Server attendance.py:372 (submit, before Bug-17) + :783 (edit, after settled-409 before H19-F1): parse_project_date → 422 if None, else data.date = jalali_date_string(...) (mutated once; all downstream — Bug-17/clash checks, uq compare, seed, SessionLog, txns, SMS text, returns — automatically canonical). Lazy today_summary import (finance pattern). After 403s (no state leak).
- App AttendanceActivity.kt:70 editSessionDate field; :103 NEW-mode display todayJalaliString; :492 submit→todayJalali / edit→original (fallback today); :411 stash details.date; :425 edit-mode display; removed 3 dead imports (SimpleDateFormat/Date/Locale — only used at old :491).
- Layout activity_attendance.xml:145: etSessionTime (+Spacer) removed, comment left; date field (1.2 weight) now fills the row.
- Traces: submit today → "1405/06/21" stored+displayed; old-client Gregorian "2026/09/12" → normalized to same → Bug-17/uq catch dupes (double-charge closed, incl. stale offline-queue items); garbage → 422; edit last-week session → details.date round-trips, no re-date, no spurious H19-F1 409. Backdating still unsupported (product call).
