# Checkpoint — M batch 1: M1/M2/M26 applied, M3 HELD (Glide conflict)

Date: 2026-09-12. main.py only, asserted + read-back. No compile/run.

## M1 (main.py:~32-41): CORS explicit + credentials off
allow_origins=["*"] -> env-driven list (CORS_ALLOWED_ORIGINS, default empty);
allow_credentials True -> False. Justified: consumers are native app (no CORS)
+ parent portal (same-origin fetch via window.location.origin, parent.py:531+).
methods/headers left "*" (minimal blast radius per order).

## M2 (main.py:~16-25): docs gated by ENV
New ENV var (default development; production disables docs/redoc/openapi).
No prior equivalent existed. .env.example documents ENV + CORS_ALLOWED_ORIGINS.

## M26 (main.py:72-80): SecurityHeadersMiddleware adds nosniff to all responses.

## M3 HELD — Glide loads /uploads/ URLs directly WITHOUT auth headers:
- InstituteSettingsActivity.kt:110-112 (logo), StudentProfileActivity.kt:266-268,
  TeacherProfileActivity.kt:279-281 (profile photos). A plain
  Depends(check_user_login) guard would break all three screens' images.
- Portal web page also references uploads/profiles/ relatively (parent.py:675).
- Awaiting user decision: signed-URL design vs alternatives (asked 2026-09-12).
