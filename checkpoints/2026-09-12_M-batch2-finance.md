# Checkpoint — M بچ ۲: ۶ مورد مالی (M10/M12/M13/M15/M16/M27)

Date: 2026-09-12. Asserted + read-back per step. No compile/run.

## قدم ۱ — M10: قفل مبلغ قسط تسویه‌شده (finance.py)
- update_installment امضا: force: bool=False و reason اختیاری (~:1211-1212).
- گیت (~:1222-1227): تغییر مبلغ قسط is_paid بدون force ← 409؛ با force ولی بدون
  reason غیرخالی ← 400. Audit همان تغییر، suffix «force روی قسط تسویه‌شده: دلیل» می‌گیرد.
- برگرداندن تسویه (True→False) دیگر paid_at را null نمی‌کند (تصمیم گزارش اولیه:
  paid_at سند تاریخی دریافت وجه است)؛ False→True مثل قبل paid_at=اکنون.
- لبه‌ی شناخته‌شده: تغییر force مبلغ ممکن است paid_amount را نسبت به مبلغ جدید
  ناسازگار کند — مسئولیت ادمین، رد آن در Audit.

## قدم ۲ — M12: اعتبارسنجی target_wallet (finance.py، submit_payment)
- (~:186-190) مقادیر غیر از teacher/institute/both ← 400 زودهنگام (قبل از هر نوشتن).
  اپ فقط همین ۳ مقدار را می‌فرستد (InvoiceActivity.kt:434-439) پس 400 امن است.
- شاخه‌ی else قدیمی (~:324-328) که «تراکنش بدون شارژ کیف» ثبت می‌کرد (و بدتر: همان
  مبلغ شارژنشده قسط تسویه می‌کرد) ← 400 دفاعی؛ هیچ «واریز بدون کیف»ای دیگر ثبت نمی‌شود.
  حالت بدون کیف نامشروع اعلام شد (دلیل در کامنت: عدم‌تطابق دفتر/کیف + تسویه‌ی مجانی).

## قدم ۳ — M13: پرداخت جزئی ← گزینه‌ی (ب) paid_amount تجمعی
- اعلام تغییر مدل (قبل از اعمال): ستون جدید Installment.paid_amount.
- چرا (ب) نه (الف): submit_payment یک اندپوینت واریز عمومی است — شارژهای کم‌مبلغ،
  رفتار طراحی‌شده‌اند (اعتبار کیف + بالانس در پاسخ)؛ خطای «جزئی پشتیبانی نمی‌شود»
  واریزهای مشروع را می‌شکست. (ب) معنای واریز را حفظ و پوشش را شفاف می‌کند.
- models.py:461 ستون paid_amount (BigInteger, default=0) + main.py:257 ورودی
  auto_patch_database با بک‌فیل «تسویه‌شده⟹پوشش کامل، وگرنه صفر» (تک‌استِیتمنت، SQLite+PG).
- حلقه‌ی تسویه (~:396-407): انباشت min(باقی‌مانده، بدهی)؛ is_paid فقط در پوشش کامل؛
  paid_at فقط همان‌موقع. تسویه‌ی دستی (~:1356) و فلیپ ادمین (:1264) پوشش کامل می‌زنند.

## قدم ۴ — M15: حذف منطقی قسط (finance.py، delete_installment)
- امضا: force: bool=False (~:1293). گیت (~:1301-1303): حذف قسط تسویه‌شده بدون force ← 409.
- db.delete(inst) ← inst.is_deleted=True (~:1311)؛ صفر db.delete(inst) باقی ماند.
  Audit همان حذف، نشان force می‌گیرد. کوئری‌های فعال از قبل is_deleted را فیلتر می‌کنند (Bug 13).

## قدم ۵ — M16: استرداد فقط واریز واقعی (finance.py، refund_transaction)
- تصمیم: session_charge از مسیر عمومی قابل استرداد نیست — مسیر درست آن
  reverse_session_financial_impacts هنگام حذف جلسه است (dependencies.py:315) که
  shareها را برمی‌گرداند. استرداد عمومی، هم کیف را دست‌نخورده می‌گذاشت، هم با
  سوزاندن is_reversed مسیر درست را برای همیشه می‌بست (آن مسیر is_reversed=False می‌خواهد).
- گیت (~:973-980): type خارج از (deposit, enrollment_payment) ← 400، قبل از تسخیر
  اتمیک (پس نه reversal ثبت می‌شود، نه چیزی می‌سوزد). settlement_payout (خروجی، مبلغ
  منفی، target_wallet=teacher!) و reversal (زنجیره‌ی undo) هم بسته شدند.
- فیکس مجاور ضروری (~:1049-1052): enrollment_payment همیشه به کیف آموزشگاه رفته
  (هر دو سایت ساخت: classes.py:418 و students.py:184) ولی target_wallet=None دارد —
  استردادش اکنون از کیف آموزشگاه کسر می‌کند (قبلاً همان تله: معکوس ثبت، کیف دست‌نخورده).
- شکاف قدیمی باقی‌مانده (خارج اسکوپ): depositهای تاریخی با کیف نامشخص هنوز بدون
  اثر کیف برمی‌گردند؛ M12 از ساخت جدیدشان جلوگیری می‌کند.

## قدم ۶ — M27: اعتبارسنجی تاریخ رسید (finance.py، submit_payment ~:191-200)
- data.date با parse_project_date (مبدل مرکزی H3، today_summary.py:138، برمی‌گرداند
  None در نامعتبر) چک می‌شود؛ نامعتبر ← 400. lazy import مثل H3-B3.
- کران منطقی: گذشته آزاد (ثبت دستی معوقه مجاز)؛ آینده مردود (رسید = وجه گرفته‌شده؛
  تاریخ آینده گزارش روزانه را فاسد می‌کند) با تلرانس +۱ روز برای اختلاف ساعت
  سرور/کلاینت (تهران/UTC) تا رسید «امروز» false-positive نگیرد.
- اسکوپ: همین اندپوینت (خط ۲۵۲ و خواهرهایش با یک اعتبارسنجی پوشش می‌خورند).
