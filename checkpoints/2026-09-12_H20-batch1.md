# Checkpoint — H20 batch 1: normalize_mobile + write points

Date: 2026-09-12. 6 files, asserted + visual read-back. No compile/run.

## normalize_mobile (dependencies.py:660-686)
FA/AR digits→ASCII (local map, no today_summary cycle), strip
space/dash/parens/+, 0098→0, 12-digit-98→0 (length-guarded so 10-digit
98... isn't eaten), 10-digit-9→11-digit-0, accept only 11/09/digits else
None. Never returns invalid string. No new imports (string ops only).

## Emptiness policy (deliberate, preserves acceptance)
STRICT (None/"" → 400, as H1 did): change_mobile (auth.py:583), admin creds
(:1565), update_teacher (:415-421), update_student.student_mobile (:472-481).
TOLERANT ("" passes through, invalid-nonempty → 400): register_student both
(:39-56), register_and_enroll both, register_teacher, update_student.parent
(:483-489), crm intake/convert/public. Rationale: "" is established-legit
(convert sets parent_mobile=""), so batch 1 only cleans formats, never
invents new rejections — except update_student.student_mobile which H1
already rejected when empty.

## Writes (all store canonical now)
- change_mobile: checks+username+teacher.mobile+log use new_mobile (:583-632).
- admin creds: clash+store+shadow+log use new_mobile (:1565-1600).
- update_teacher: clash on mob + post-loop overwrite (:442-443).
- update_student: NEW clash check on student_mobile (:477-480, update_teacher
  pattern/message); parent explicitly clash-free (shared siblings); post-loop
  overwrites (:502-505). Post-loop overwrite chosen over pydantic mutation
  (v2-safe).
- register_student: uniqueness on normalized + parent_mobile override ADDED
  (:57 — was fully raw via dict()).
- Teacher shadow (auth.py:84-92): canonical username; legacy-raw lookup
  fallback + one-time heal, role-guarded (pathological-collision safe).
  Canonical-stored behavior identical to before.

## Left for batch 2 (reads) + backfill
current_mobile compare (:599), all login/OTP/identity lookups, select_child
:202 double-side, H14/OTP-bucket keys. Legacy rows stay readable (no lockout)
until backfill; new writes are canonical from now on.

## Step-4 traces (mental, no run)
"+98 912 345 6789" → strip → "+989123456789" → "989123456789" → 12-digit-98
rule → "09123456789" → valid. "12345678" → no rule → len 8 → None → 400
everywhere, nothing stored. "9123456789" (H1-dual-form) → "09123456789".
"۰۹۱۲۳۴۵۶۷۸۹" → digit-map → valid.
