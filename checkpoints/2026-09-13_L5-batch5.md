# L5 Batch 5 — checkpoint (2026-09-13)

## Scope
12 files under `KharazmiAdmin/app/src/main/java/com/example/kharazmiadmin/`:
ClassDetail, ClassSetup, AddClass, DeletionRequests, Calendar, Exam, Homework,
TransactionManage, Chart, EditStudent, CrmLeads, PersonList.

## Result
- **313 replacements** (ClassDetail 73, ClassSetup 34, AddClass 11, DeletionRequests 27,
  Calendar 21, Exam 27, Homework 25, TransactionManage 22, Chart 18, EditStudent 20,
  CrmLeads 18, PersonList 17).
- **strings.xml: +305 entries** (`cdetail_*`, `csetup_*`, `aclass_*`, `delreq_*`, `cal_*`,
  `exam_*`, `hw_*`, `txn_*`, `chart_*`, `estudent_*` incl. 7 `estudent_sum_*`, `crm_*`, `plist_*`).
  XML-validated; R.string resolve check: NONE missing.
- Fully ZERO remaining: DeletionRequests, Chart, EditStudent, CrmLeads, PersonList.

## Deliberate leftovers (45 regex hits, all classified)
- ClassDetail :289 `"دوازدهم"` — hardcoded API value in updateClassInfo (looks like a BUG:
  grade always sent as دوازدهم). :666 `"حاضر"` — server-field compare. SET ASIDE.
- ClassSetup :133 `"، "` separator (punctuation, left like batch-4 `", "`); discount trio
  (`بدون تخفیف/درصدی/مبلغ ثابت` :197/:200/:211/:212/:275/:276) — deferred unit (spinner+compare+logic).
- AddClass :68-74 grades/eduTypes arrays + :144-146 (`مختلط/نامشخص`) — sent as
  grade_level/education_type/request fields. SET ASIDE (API-bound).
- Calendar :249 days array + :251 default — sent as days_of_week. SET ASIDE.
- Exam :375 `"، "` separator. Homework :291-292 mock student names. SET ASIDE.
- TransactionManage :243 `"اصلاح شده"` — inside a code COMMENT (out of L5 scope).

## Preserved byte-identical
- DeletionRequests :102 «درخواست حذف بازی وجود ندارد» (possible typo, NOT fixed).
- csetup :223 reuses `sreg_tuition_preview`; «حذف»→`action_delete` (csetup/txn);
  «ذخیره»→`action_save` (txn); «بله، ثبت شود»→`invoice_confirm_yes` (aclass);
  estudent name/family→`treg_name_req`/`treg_family_req`; plist معلق/فعال→`tprof_*`.
- Bare `%` escaped as `%%` (cdetail att_rate/att_pct, exam rep_avg).
- H16/IS_MODIFIED flow untouched (txn :238-248 logic identical, only Toast moved).

## Traps hit
- Exam :308 inner quotes inside `${}` need NO backslash in source (`"نمره شما: "` not `\"`).
- ET.indent needs `space="    "` (str) on this Python, not int.

## Running totals
Batches 1–5: ≈893 migrated of 1204 (≈74%). Remaining ≈311 incl. set-asides.
Batch 6 (planned): Report / Settings / ClassManagement / InstituteSettings + rest (~255).
