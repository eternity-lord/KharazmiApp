# E2E admin test (2026-09-14, uvicorn main:app on /tmp COPY; original hash-identical before/after; /tmp cleaned; NO fixes applied per instruction)

## Verdict: 60+ PASS, 5 REAL bugs (1 mine), 0 unexpected 500s besides the confirmed one
Server log: 2× HTTP 500 (same keyed-'both' bug) + 4 chained Traceback headers; nothing else.

## REAL BUGS (report-only, await fix orders)
1. HIGH: suspended teacher CAN log in (200) — auth.py User/shadow branch checked first has no
   is_suspended gate (only the Teacher branch has it). Response role=admin/sub_role=teacher.
2. HIGH: keyed target='both' payment → 500 UNIQUE idempotency_key — finance.py:399-402 stamps
   ONE key on TWO leg rows; my sqlite-uq unique index now enforces one-row. DESIGN DECISION
   NEEDED: (a) non-unique index (dupes by design) or (b) per-leg key suffix. Unkeyed 'both' + keyed
   single-leg work; replay honesty verified (duplicate:true, no new row — txn9 was installment
   collection, first misread as double-charge, disproven via descriptions+allocations).
3. HIGH (mine, Y4): finance.py:654 check_student_access(student_id, authorization, sub_role, db) —
   args 3+4 SWAPPED vs signature (…, db, sub_role) → class_status 403 for EVERYONE incl. admin.
   All Y1 sites verified correct order; only this one wrong. Blocks step-5 reads via that endpoint
   (worked around via class-details + debtors).
4. MEDIUM: suspended student's parent GETS OTP (200, 3× proven); parent.py request_otp AND login
   have ZERO suspension gates (grep-proven). Black-box login proof impossible (OTP stored hashed).
5. LOW: GET class details on soft-deleted class → 200 with is_deleted=True (no 404 filter).

## Money verified (hand-checked numbers)
Tuition 1M, pay 300k → debt 700k/paid 300k; wallets 160k = 300k−140k session charge ✓.
Session: costs 200k/80k + penalties 100k/40k; charges 3×140k = 420k ✓. Settlement 300k = 200k+100k ✓.
Refund both legs → paid 0/debt 1M/enrollment 0 + installment auto-coverage UNWOUND (50k→0, paid→unpaid) ✓.
Installment ledger: payment auto-allocated 100k→inst#1; manual pay collected rest as new receipt ✓.
Guards: resettle 400, edit/delete-settled 409, paid-inst update/delete 409 (force+reason OK),
suspended class 403s, over-cap approve WARNS (not blocks), version-409 + version-required-400.
Reports: financial_summary/debtors/analytics consistent with detail reads. Remind+broadcast OK.
Admin-only reachability confirmed (refund/reset-password/deletion-approve/credentials).

## Test artifacts (not server bugs)
Rate limiters (login 5/5m, register 5/h, OTP) required waits+fresh server; teacher login lacks
sub_role key (obs); payments-history endpoint reads online-Payments table (empty = correct);
branch fixture (branch=1) applied on COPY (no API assigns branches — observation).
