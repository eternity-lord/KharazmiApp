# E2E 5 bugfixes (2026-09-14; verified on /tmp COPY; original hash-identical; no Android)

## B3 (mine): finance.py:654 — check_student_access arg order fixed (…, db, sub_role).
Verified: class_status 200 with correct splits (150k+150k).
## B1: auth.py User branch — teacher shadows (role=teacher) now pass H10 gates
(is_approved + is_suspended) via Teacher lookup. Root cause: User-first lookup predates H10;
shadow created on first login → all later logins bypassed the Teacher branch. Student/parent
shadows safe (random passwords). Verified: suspended teacher login → 403 (was 200).
## B4: parent.py — H10 gated only auth.py's student family; parent_mobile family was missed
(not a regression). Added: request_otp all-suspended→403; login single-suspended→403;
multi: suspended filtered from list, all-suspended→403; select_child suspended→403
(defense in depth: temp token may predate suspension). Verified: 403/200/… as designed.
## B2: idempotency — chose (A) non-unique index. Writer (2 legs, 1 key) + reader (multi-row,
sum-check) agree on operation-id semantics; unique was the over-constraint (mine). Changes:
models.py:273 unique→index (+comment), main.py: uq index dropped, ix_ plain index + DROP IF
EXISTS interim cleanup. Verified: keyed 'both' → 200, 2 rows; replay → duplicate:true, rowcount stays 2.
## B5: classes.py details + 2 twins (full_report, students_full) — is_deleted=False → 404.
Verified: details + students_full 404 on deleted class.
## Verify: ast ×6 + IMPORT CLEAN + server log zero Traceback. Self-caught: botched def rename
in B5 script (duplicated get_class_details) — fixed immediately, grep-verified.
