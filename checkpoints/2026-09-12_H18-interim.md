# Checkpoint — H18 interim hardening (qr_check-in)

Date: 2026-09-12. 4 ops in routers/attendance.py, anchors asserted + visual
read-back. No compile/run.

## 1. Same-day gate (:934-937)
After sess-404, before enrolled check: lazy `parse_project_date(sess.date)`
(H3 converter, Jalali/Gregorian) must equal `utcnow().date()`, else 403 with
the requested message. Fail-closed: unparseable date (None) also 403s.

## 2. Client expires_at REMOVED (:926-929 slot)
Old `if req.expires_at < now_ts: 400` deleted — client-controlled, any future
date bypassed it, false security impression. Model field KEPT as-is
(required, accepted-but-ignored like token/salt) → zero schema breakage for
any out-of-workspace caller.

## 3. In-memory per-student rate limit (:879-895 helper, :928-929 call)
5 attempts / 5 min / student_id, 429 on exceed. Placed BEFORE sess lookup so
wrong-code guesses consume quota (anti-brute-force on sequential codes).
LoginAttempt table deliberately NOT reused (semantically "login attempts");
no migration. Resets on restart, per-process — accepted for interim.

## 4. Doc comment (:904-909)
States token/salt/expires_at are client-supplied + unvalidated (no real QR
producer in workspace) and this is interim until the feature's fate
(real consumer vs redesign/remove) is decided.

## Order now
identity → suspend(H10) → rate-limit(429) → sess-404 → same-day(403) →
enrolled(403) → dup(400) → Present.
