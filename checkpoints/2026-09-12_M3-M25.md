# Checkpoint — M3 (authed uploads + Glide headers) + M25 (upload IDOR + magic bytes)

Date: 2026-09-12. Asserted + read-back. No compile/run.

## M3 server (main.py:~80-98): StaticFiles mount REPLACED by GET /uploads/{filename}
- Behind check_user_login; traversal guards (no / or backslash, basename,
  no dotfiles); profiles/ dir only (homework/messages/exams live under
  absolute /home/user/uploads/* and were never mount-served — unchanged).
- Imports: FileResponse added, check_user_login added, StaticFiles removed.

## M3 app: GlideUrl + LazyHeaders (Bearer USER_TOKEN) x3
- InstituteSettingsActivity.kt:~111-121 (logo), StudentProfileActivity.kt
  (~266+), TeacherProfileActivity.kt (~279+). Unique var names
  (glideAuthPrefs/Token) avoid collisions. Empty token -> 401 -> existing
  .error() placeholder (graceful). Zero .load(imgUrl) left.

## M3-portal: NOT implemented (ordered: investigate + propose)
- Portal authed via localStorage parent_token + fetch Bearer (parent.py:538/656);
  avatar is plain <img src> (:675) which cannot send headers.
- PROPOSAL (no server change): JS blob-fetch — fetch(url, {Authorization}) ->
  blob -> URL.createObjectURL -> img.src. Rejected: query-token (log leaks),
  cookies (no server cookie support exists).

## M25: upload IDOR + magic bytes
- dependencies.py: validate_image_upload (ext + PNG/JPEG/WEBP magic, 400s).
- students.py:517 upload_student_photo: admin/secretary OR self (student role +
  get_session_student match, JWT-verified session); magic via helper.
- teachers.py:476 upload_teacher_photo: admin/secretary OR self (teacher role +
  get_logged_in_teacher match, lazy import like auth.py); magic via helper.
- Homework/messages validators (PDF/docx scope) deliberately untouched.

## Step-4 traces
- Admin uploads teacher photo: sub_role admin -> pass; real JPG passes magic.
- Teacher-self: teacher_id match -> pass; other teacher's id -> 403.
- Student-self via student app (future): own.id match -> pass.
- Renamed .exe -> .jpg: ext ok, magic fail -> 400, nothing written.
- Glide screens: same URLs + Bearer -> 200; expired token -> placeholder.
