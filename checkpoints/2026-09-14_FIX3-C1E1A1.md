# FIX3: F-C1 + F-E1 + F-A1 (2026-09-14; live 4/4; hash STABLE aefdf3c8; /tmp cleaned)

## Changes (all serial, one edit at a time)
1. F-C1 routers/classes.py add_enrollment (~:426-452): single commit — enrollment_id set at
   construction (id available post-flush); added branch_id (H7: student else course), course_id,
   target_wallet at construction, remittance_number via get_next_sequence_value(db,
   "remittance_institute", 100001). Second commit deleted.
2. F-E1 requirements.txt: +reportlab>=3.5.0 (no PDF lib installed; both endpoints use reportlab
   APIs directly — rewrite bigger than dep). Installed: reportlab 5.0.1.
3. F-A1 routers/analytics.py /analytics/classes (:353): check_user_login →
   check_admin_or_secretary_access (sibling parity with /analytics/teachers).

## Verify
ast.parse ×2 OK; real `import main` CLEAN (DATABASE_URL=/tmp copy ONLY — lesson held);
live 4/4: enroll-paid txn fully linked (EID, branch 1, rem 100001, course, institute);
refund unwinds total_paid 300000→0 (leak closed); report_card/pdf → 200 real %PDF bytes;
/analytics/classes teacher→403 + admin→200. Zero Tracebacks. Prod hash unchanged.
