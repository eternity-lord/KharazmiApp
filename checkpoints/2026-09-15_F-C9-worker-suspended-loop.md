# F-C9 — لوپ retry بی‌پایان ورکر روی کلاس معلق — 2026-09-15

## مشکل
ورکر auto-end هر ۶۰ ثانیه جلسه‌های LIVE گذشته از آستانه را claim و finalize می‌کرد؛ روی کلاس
معلق، گارد H10 داخل submit هر بار 403 می‌داد → rollback → برگشت به LIVE → تیک بعد دوباره.
نتیجه: لوپ بی‌پایان + اسپم لاگ، جلسه هیچ‌وقت بسته نمی‌شد.

## تصمیم: گزینه (ب) — بستن مستقیم بدون submit
- فلسفه H10: ثبت جلسه روی کلاس معلق اصلاً نباید اتفاق بیفتد → دورزدن کامل submit (حتی صفرحاضر) وفادارترین اجراست.
- جلسه واقعاً تمام می‌شود: بدون ENDED، ردیف LIVEِ ارواح start_live بعدی را 409 می‌کرد و داشبورد معلم «جلسه جاری» کهنه نشان می‌داد.
- گزینه (الف) رد شد: تعلیق طولانی/دائمی = LIVE ابدیِ غیرقابل‌بستن (مسیر دستی هم روی معلق 403 می‌دهد)؛ «حفظ راستر» هم موجه نیست چون فعالیت حین تعلیق طبق H10 نامشروع است.

## فیکس‌ها (سریال، فقط main.py)
- `main.py` (:541-542): ایمپورت lazy `Course` و `_now_str` در `_loop`
- `main.py` (:554-580): قبل از claim، کوئری تازه `course.is_suspended` در همین تیک/سشن (نه کش)؛
  اگر معلق: UPDATE مشروط (H8-P4) یک‌ضرب `LIVE→ENDED + ended_automatically + end_time` + کامیت،
  بدون finalize/submit. rowcount صفر (تسخیر هم‌زمان دستی) → رد شدن؛ finalize دستی روی معلق
  403 می‌دهد و به LIVE برمی‌گردد و تیک بعد ورکر می‌بندد — در هر ترتیب‌اجرایی همگراست.
- رفتار کلاس حذف‌شده (None) و کلاس عادی بدون تغییر.

## راستی‌آزمایی (فقط /tmp کپی، DATABASE_URL=/tmp)
- AST OK؛ import main CLEAN؛ ادمین فقط روی کپی (prod users=0 تأیید شد)
- TEST-S (معلق): start_live → suspend (SQL) → بک‌دیت ۲۰۰ دقیقه → ورکر واقعی در ~۵۰ثانیه:
  ENDED + auto=1 + end_time ست، **صفر** session_logs، تراکنش‌ها ثابت → PASS
- TEST-N (عادی، دست‌نخورده): همان شرایط بدون تعلیق → ENDED + auto + سشن صفرحاضر (مسیر F-C6) → PASS
- پایداری: ۷۰ ثانیه اضافه (یک تیک کامل) → هر دو همچنان ENDED، صفر سشن جدید، صفر «خطا در بستن جلسه»، صفر Traceback → PASS (لوپ مرده، ساختاری: ورکر فقط LIVE را select می‌کند)
- teardown: هش f0e55fe7… قبل/بعد پایدار؛ سرور stop؛ /tmp پاک

## یافته‌های جانبی
- چاپ‌های `print` ترد ورکر در محیط tee به‌خاطر بافر stdout گم می‌شوند (حتی بعد از stop؛ SIGTERM بافر را دور می‌ریزد) — سند رفتار = حالت دیتابیس، نه لاگ. پیشنهاد (انجام نشد، خارج از اسکوپ مینیمال): `flush=True` روی printهای ورکر برای observability پروداکشن.
- تأیید ضمنی: مسیر دستی end_live روی کلاس معلق همچنان 403 می‌دهد و جلسه را LIVE نگه می‌دارد (رفتار H10، دست‌نخورده) — تنها بسته‌کننده، ورکر است.
