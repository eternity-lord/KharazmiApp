# Checkpoint — L بچ ۴ (آخر): اپ اندروید (L1-app/L5/L6/L9/L10)

Date: 2026-09-12. Asserted + read-back per step. No compile/run.

## قدم ۱ — L1 اپ: ۳۱ printStackTrace ← Log.e (۱۷ فایل، صفر باقی‌مانده)
- جایگزینی مکانیکی با `android.util.Log.e("<کلاس>", "<fun> failed", e)` — نام
  تابع محاط از روی سورس استخراج شد؛ FQ بدون نیاز به import (۳ مورد inline بعد
  از throw هم پوشش خورد). فایل‌ها: AddClass(1)، Attendance(1)، CacheManager(4)،
  Chart(1)، ClassSetup(3)، Invoice(6)، Login(1)، Main(1)، NotificationCenter(1)،
  PendingAttendanceStore(1)، PendingTeachers(1)، Sms(2)، StudentRegister(2)،
  TeacherCredentials(1)، TeacherDashboard(2)، TeacherProfile(2)، TeacherRegister(1).

## قدم ۲ — L5: تحلیل (بدون فیکس)
- نظر: debt جدا، نه بخشی از دیباگ. ۶۹ فایل/هزاران رشته، صفر باگ رفتاری، دیف
  غول‌پیکر با ریسک رگرسیون رشته‌ها و هزینه‌ی ریویو بالا؛ strings.xml (۲۰۰ رشته)
  نشان می‌دهد مهاجرت تدریجی ممکن است ولی یک‌باره‌اش در اسکوپ تثبیت نیست.
  پیشنهاد: فقط رشته‌های جدید/تغییریافته در strings.xml (قانون از این به بعد).

## قدم ۳ — L6: NewInvoiceApi تکراری (ui/MainRepository.kt:11/41)
- identical نبودند: نسخه‌ی ApiInterfaces.kt:35 اینترفیس واقعی رتروفیت (۴
  اندپوینت، مصرف: InvoiceActivity) و نسخه‌ی ui تک‌متده‌ی بدون انوتیشن (مصرف:
  MainRepository که خودش dead code است — صفر مصرف خارج از ui/).
- درست: نسخه‌ی اصلی. فیکس: rename نسخه‌ی ui به ClassStatusApi (اعلان+مصرف+
  کامنت) تا سایه‌ی هم‌نامی از بین برود؛ بدون تغییر رفتار. حذف فایل‌های dead
  ui جداگانه تصمیم گرفته شود (خارج اسکوپ).

## قدم ۴ — L9: ضد دابل‌کلیک (۶ تابع در ۵ فایل + راستی‌آزمایی ۳ صفحه‌ی کلیدی)
- DebouncedRequest برای دکمه مناسب نیست (دیباونس ۴۰۰ms جست‌وجوست؛ روی submit
  یعنی تأخیر/دراپ) پس الگوی isEnabled/loading سبک-رجیستر تعمیم داده شد:
  SubmitGrade.sendGrade، Sms.sendSms (خطا بدون Main بود ← post به Main)،
  Message.sendMessageToServer/sendBroadcastOnServer، EditTeacher.saveChanges،
  EditStudent.saveDataToServer — همه با بازگشت در مسیر ماندگار (موفقیتِ finish
  بدون بازگشت). لاگین/ثبت‌نام‌ها/پرداخت Invoice از قبل گارد داشتند (تأیید شد:
  GoldButton واقعی در لی‌آوت Invoice). TransactionManage با دیالوگ تأیید
  one-shot پوشش دارد (بدون تغییر).

## قدم ۵ — L10: چک‌باکس remember-me (activity_login.xml:196 + LoginActivity)
- چک‌باکس «مرا به خاطر بسپار» (پیش‌فرض تیک‌نخورده)؛ ذخیره‌ی امن فقط با تیک،
  بدون تیک بقایای SAVED_* امن پاک می‌شود. نکته‌ی ظریف: ورود بیومتریک تیک را
  می‌زند (:148) وگرنه همان لاگین موفق، ذخیره‌ای را که با آن وارد شده پاک می‌کرد.
