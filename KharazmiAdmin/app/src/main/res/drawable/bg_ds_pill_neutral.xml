<?xml version="1.0" encoding="utf-8"?>
<!-- DESIGN SYSTEM: پیل وضعیت (neutral) — رنگ + برچسب متنی (هرگز فقط رنگ) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/ds_neutral_container" />
    <corners android:radius="@dimen/corner_radius_pill" />
    <stroke
        android:width="@dimen/stroke_hairline"
        android:color="@color/ds_border_strong" />
</shape>
