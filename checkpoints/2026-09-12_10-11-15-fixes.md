# #10 SMS + #11 4x read-IDOR + #15 update_student — 2026-09-12 (no compile/run)
- #10 admin.py:146 send_sms → check_admin_or_secretary_access. Compat: sole launcher Main (admin/secretary hub); nav_sms visible to secretary; endpoint is group-only (SmsSendRequest has no single-target mode).
- #11a admin.py:198 pending teachers (w/ mobile) → admin_or_secretary. Compat: Main-only launcher; secretary superset harmless (approve_teacher already allows secretary H10-S2).
- #11b attendance.py:619-649 student_history: 404s kept first (H16 pattern), then admin/secretary bypass + owning-teacher (course.teacher_id via get_logged_in_teacher) + self/parent (qr-precedent session dance + guarded split). DEVIATION: owning-teacher added — ClassDetail:621 (reachable from TeacherDashboard) legitimately calls it; spec's list would have broken teachers.
- #11c attendance.py:~665-713 session_details: admin/secretary + owning-teacher only (AttendanceActivity ← Main + TeacherDashboard; portals never call it → students/parents 403).
- #11d finance.py:1336-1368 installments: staff full; student/parent scoped (enrollment→404/403 ownership, student_id→equality, none→inject self). No app caller (portals read profile.installments) → zero break. Parent multi-child inherits first-only semantic.
- #15 students.py:452 lock removed (audit pattern) + :506-532 conditional UPDATE (coalesce(version,1)==data.version, version=data.version+1, None→2 preserved, non-column keys ignored like before) + rowcount→409 same message. Normal path byte-identical end-state.
- Self-repair this turn: FIX comments initially placed inside single-line def parens (SyntaxError) → moved after `):`; ast.parse OK x4.
