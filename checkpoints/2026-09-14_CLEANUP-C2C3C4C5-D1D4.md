# CLEANUP-BATCH — F-C2/F-C3/F-C4/F-C5 + D1–D4 (+F-C7 یافته زنده) — 2026-09-14

## Edits (سریال، grep-verify شد)
سرور:
- `routers/crm.py` online-reg txn: `branch_id = student.branch_id or course.branch_id` (H7) روی تراکنش شهریه (F-C2a)
- `routers/students.py` register_and_enroll txn (F-C2b): همان H7
- `routers/finance.py` create_installment (F-C3a): commit→flush؛ کامیت واحد آخر با ActivityLog
- `routers/crm.py` convert_lead (F-C3b): ثبت دانش‌آموز+enroll قبل از کامیت واحد؛ تشخیص: پنجره کرش (نه race) — بدون پول
- `routers/attendance.py` /attendance/get (F-C4): نرمال‌سازی req.date با parse_project_date/jalali_date_string (lazy)؛ نامعتبر→[] ؛ get_history دست‌نخورده
- `routers/admin.py` /test/transaction_logic (F-C5): ENV=production→404 (الگوی main.py M2)
- `routers/crm.py` (F-C7، یافته زنده حین تست): افزودن `from sqlalchemy import or_` — کل مسیر ثبت‌نام آنلاین ۵۰۰ می‌داد (NameError زمان اجرا؛ import-main نمی‌گیرد)
اندروید (بدون کامپایل): strings.xml (biometric_error + ۳ خطای پرداخت)، LoginActivity.kt:133، activity_login.xml (حذف مرده‌ها)، TeacherProfileActivity.kt نگهبان isEnabled ×۳، InvoiceActivity.kt مپر خطا (۴۲۹/۵xx/شبکه)

## Verify (فقط DATABASE_URL=/tmp کپی)
- AST هر ۶ فایل سرور OK؛ import main CLEAN
- لایو ۸/۸: F-C2a شعبه=۱؛ F-C2b شعبه=۱+enroll لینک؛ F-C3a قسط+لاگ؛ F-C3b تبدیل با درس (۱ enroll + REGISTERED)؛ F-C4a میلادی→۱ رکورد + نامعتبر→[] ؛ F-C5 دولوپ‌۲۰۰ + پروداکشن‌۴۰۴
- صفر Traceback (۳ سرور)؛ هش aefdf3c8… پایدار؛ سرورها stop؛ /tmp پاک
- اندروید: ارجاع‌ها سازگار (۴ استرینگ تعریف، ۱+۳ مصرف، ۳ نگهبان)

## درس‌ها
- ۱۴۰۴/۰۶/۲۱ = ۲۰۲۵/۰۹/۱۲ (نه ۲۰۲۶!) — حساب سال میلادی از روی شمسی
- ریت‌لیمیت لاگین ۵/۵دقیقه: توکن ادمین را از جدول user_sessions کپی بخوان، لاگین نزن
- NameError مسیر اجرا (مثل or_) را فقط تست لایو می‌گیرد، نه import-main
