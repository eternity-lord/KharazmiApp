# Checkpoint — گارد caller برای ۳ بحرانی audit-v2 (نمره/جلسه/کلاس)

Date: 2026-09-12. Asserted + read-back. No compile/run.

## قدم ۱ — submit_grade (students.py:18 ایمپورت، :237 امضا، :243-251 گارد)
- الگوی مرجع عیناً: `require_permission("grade.write")` در امضا (مثل exams.py:49) +
  `get_logged_in_teacher` + تطبیق `Course.teacher_id` (مثل exams.py:52-56).
- تنها دلتای مجاز: گذر ادمین/منشی (شکل _verify_live_course_teacher) — چون الگوی teacher-only
  مرجع، اصلاح نمره توسط ادمین (مسیر مشروع اپ) را می‌شکست؛ require_permission به‌خودی‌خود
  شاگرد/ولی/temp را رد می‌کند (grade.write فقط ادمین/منشی/معلم).
- عمداً بدون چک عضویت شاگرد در کلاس (مرجع هم ندارد؛ قدم ۴).

## قدم ۲ — submit/edit جلسه (attendance.py:48-55 تعمیم، :353-356/:709-715 امضا، :364-367/:724-726 گارد)
- `_verify_live_course_teacher` مستقیم صدا زده شد + پارامتر اختیاری `action_label`
  (پیش‌فرض «جلسه‌ی زنده»؛ ۳ کالر live در :157/:214/:271 بی‌تغییر ماندند).
- هر دو امضا `authorization`+`sub_role` گرفتند؛ گارد fail-fast بلافاصله بعد از 404 کلاس
  (قبل از H10/409/422) تا غیرمجاز وضعیتی لو ندهد.

## قدم ۳ — update_class_info (classes.py:1070-1076 امضا، :1081-1090 گارد)
- نظر: ادمین/منشی + معلمِ مالک (نه ادمین‌فقط) — چون ClassDetail اپ (قابل‌دسترس معلم از
  TeacherDashboard) همین اندپوینت را صدا می‌زند و ادمین‌فقط مسیر مشروع را می‌شکست.
- باقی‌مانده‌ی آگاهانه: grade_level روی تعرفه اثر دارد → تغییرش توسط معلم follow-up می‌خواهد (audit).

## قدم ۴ — ردیابی (خلاصه؛ کامل در گزارش چت)
- نمره: معلم‌مالک/ادمین/منشی ← همان رفتار قبل؛ شاگرد ← 403 از permission؛ معلم‌نامالک ←
  403 مالکیت؛ temp ← 401. (قبل: همه 200.)
- جلسه (ثبت/ویرایش): مالک/ادمین/منشی ← ادامه‌ی دقیقاً قبلی؛ شاگرد/ولی/نامالک ← 403
  پیش از هر شارژ/تغییری. (قبل: ثبت موفق + شارژ کیف.)
- کلاس: ادمین/منشی/مالک ← موفق؛ شاگرد/نامالک ← 403. (قبل: rename موفق.)
- تغییر precedence (403 مالکیت قبل از 409/422) فقط تماس‌گیرنده‌ی غیرمجاز را لمس می‌کند.
